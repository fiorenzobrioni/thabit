# PLANNING.md — Piano di realizzazione thabit

Piano di sviluppo per **thabit**, habit tracker Android (Kotlin + Jetpack Compose) con UI in stile code editor + CI/test suite (tema "Obsidian Syntax", ereditato dalla serie). Ogni passo è smarcabile: `[ ]` da fare → `[x]` completato.

Riferimenti: `VISION.md` (identità, metafora CI, schermate, metriche, decisioni prese studiando il campo — Loop, Streaks, Habitify, HabitNow, Habitica), i repo gemelli [tweather](https://github.com/fiorenzobrioni/tweather) e [tsteps](https://github.com/fiorenzobrioni/tsteps) come sorgente dei componenti UI e delle decisioni già validate su device (design system in `obsidian_syntax/DESIGN.md` di tweather; il porting più recente dei componenti è quello di tsteps, che è quindi la sorgente preferita per la copia).

Regola del piano (di serie): ogni decisione e ogni deviazione dalla vision va registrata qui **con la motivazione**, nella fase in cui è stata presa.

### Revisione della VISION — agosto 2026 (fine Fase 1)

Giro di critica alla VISION a Fase 1 chiusa. Le decisioni sono distribuite nelle fasi che le implementano; qui l'indice con il perché, perché è il tipo di ragionamento che si perde per strada.

**Accolte in VISION:**

1. **`no run` — l'app può dire "non lo so"** (§3.3.8, §7, §4.3): senza una riga di presenza, l'assenza di check significa insieme "ho fallito" e "il telefono era spento", e con i verdetti calcolati in lettura il rientro da nove giorni offline produce nove `✗ build failed` di dati inventati — la bugia che §1.1 vieta. Una build che non è mai partita non è una build fallita: Jenkins non colora di rosso i giorni in cui nessuno ha pushato. Fase 2 (dominio), 8 (coverage), 6 (nessun commit).
2. **Presenza scritta solo da interazioni deliberate** (§7): la proposta originale includeva il render del widget, che però si ridisegna da solo al rollover — la presenza sarebbe stata automatica ogni giorno a telefono acceso, e l'avrebbe scritta il worker, che per contratto §6.8 non scrive mai. Correzione: apertura app, tap sul widget, tap su azione di notifica. Fase 2, 10.
3. **`no run` rompe la streak, non la health** (§3.3.8): la simmetria con lo skip era la scelta ovvia ma regalava il gaming peggiore — chiudere l'app nei giorni difficili per non perdere mai la catena. Asimmetria voluta e coerente con §3.3.3: streak severa (catena di pass digitati), health indulgente. Fase 2.
4. **`perfect-week` richiede copertura piena** (§5): con i `no run` fuori dai denominatori, una settimana con due soli giorni girati e passati sarebbe stata "100%". Un record va guadagnato, non ottenuto per assenza. Fase 2, 8.
5. **`habits.test` al posto di `habits_test.yaml`** (§4.1): `- [x] meditate 10 min` non è YAML valido (uno scalare plain non può seguire una flow sequence), e in una serie il cui primo comandamento è "the file must not lie" un file che dichiara una grammatica e non la rispetta era l'unico punto in cui la serie veniva meno a sé stessa. `.test` è il formato del runner, come `.diff` è un formato e non un linguaggio; `#` come canale commenti resta giustificato. `settings.config` invece era già a posto (JSON con `//` è JSONC, formato reale, precedente di tsteps). Fase 1 (rename), 3.
6. **`[·]` per gli avoid test** (§4.1, §4.6): `[ ]` significava "pending" per un boolean e "sta reggendo" per un avoid — nel file li distingueva il commento, sul widget nulla. Fase 1 (tokenizer), 3, 10.
7. **Coverage e regressions in `stats.md`** (§4.3): §4.3 si intitolava *coverage report* senza contenere una metrica chiamata coverage — ora ce l'ha, ed è il contrappeso onesto a `no run` (non aprire mai l'app ferma il rosso, ma la barra di copertura lo dichiara). Le regressioni sono distinte dai flaky (flaky = non è mai stato solido; regression = *era* solido e si sta rompendo) e sono la riga più azionabile che l'app possa produrre. Fase 2, 8.
8. **Finestre statistiche ancorate a `createdAt`** (§4.3, §5): un test aggiunto ieri sarebbe apparso come "3% pass rate" fra i flaky il giorno dopo. Fase 2, 8.
9. **Finestra di amend definita** (§3.3.5): "ieri resta editabile" non è una durata. È il giorno logico precedente, editabile fino al `day_ends` di oggi — la grazia si consuma col passare della giornata; al rientro da più giorni si amenda solo l'ultimo giorno chiuso, gli altri sono `no run` e restano tali. Fase 2, 6.
10. **Wizard con default e uscita anticipata** (§4.5): "tre abitudini in meno di un minuto" (§9) con sei prompt obbligatori sono diciotto risposte in sessanta secondi, cioè un criterio di successo falso. Tutto tranne `name:` ha un default, `[done]` chiude subito, `[more]` continua. Fase 5.
11. **Giorno logico dichiarato quando diverge dalla data di parete** (§4.1): con `day_ends: "03:00"`, all'una di notte il telefono dice il 21 e la suite è quella del 20 — non dichiararlo sembra un bug. Fase 3.
12. **Skip con `until:`** (§4.1): la settimana fuori casa è il caso più frequente della categoria dopo `day_ends`. Fase 3, 5.
13. **Export della presenza** (§7): coverage e `no run` devono essere ricalcolabili dall'export come tutto il resto, o sono formule segrete. Fase 11.

**Registrate in VISION come progettate-ma-differite** (§6.9, §6.10): il runner guidato `$ thabit run` con il FAB che passa da `+` a `▶`, e i profili di suite come branch git (`git checkout vacation`) con `⎇` tappabile. La critica al FAB è giusta e sta agli atti — `+` è un verbo di setup, mentre i gemelli hanno il glow su un verbo quotidiano — ma il file principale **è già** il runner (§3.3.2: un colpo d'occhio, un tap) e il widget copre già i boolean: una terza superficie per gli stessi check va chiesta dall'uso reale, non dalla simmetria dei verbi — la regola con cui la stessa §6.2 ha parcheggiato il timer. Valutazione in Fase 12 con dati d'uso. Liberato il nome, il timer per-test diventa `$ thabit watch` (watch mode), che è anche il nome giusto.

**Respinte, con motivazione registrata:**

- **Un solo alarm riarmato al posto dei reminder per-test** (proposto come coerenza con "one shared job"): gli alarm inesatti sono batchati dall'OS, quindi i *fire* sono identici e quindici registrazioni pendenti costano quanto una — il risparmio di batteria non esiste. In compenso una catena che si riarma a ogni fire perde **tutti** i reminder su un fire mancato, mentre alarm indipendenti degradano un test alla volta. La regola di serie riguarda i job periodici di polling, non gli alarm event-driven. Registrata in VISION §7 e in `CLAUDE.md` perché non venga riproposta. Fase 9.
- **Quota fuori dall'MVP**: il rischio di scope è reale (una quota `3/week` tocca verdetto, denominatori, streak, health, intensità heatmap e separatore di settimana), ma togliere le quote amputerebbe i separatori settimana, che sono identità. Mitigazione scelta: **si prototipa per prima** nel dominio puro della Fase 2, prima di tutto il resto dei verdetti. Fase 2.
- **Sensori che passano i test** (§6.11): non era in discussione, ma va agli atti — tsteps scrive i passi su Health Connect, quindi `it("walks 8000 steps")` potrebbe diventare verde da solo. Il senso di quest'app è che la spunta l'ha digitata una persona.

### Revisione accessibilità ai non-professionisti — agosto 2026

Secondo giro di critica: l'app è usabile da chi non riconosce la metafora CI? La diagnosi accolta è che **estetica e vocabolario sono due cose diverse** — il look da editor (monospaziato, gutter, controlli testuali) non chiede nulla al lettore, è una checklist ben vestita, e il gesto quotidiano è il tap che fanno tutte le app della categoria; è il *vocabolario tecnico* che può escludere, e sono meno di dieci parole. La cura non richiede di annacquare niente, ma **non è quella proposta**.

**La correzione di rotta.** La proposta era glossare le stringhe difficili nel canale commenti (`~ build unstable (4/6)` seguito da `# 4 test su 6 passati`). Verificato sui gemelli prima di decidere: il `values-it` di tsteps ha 65 righe e localizza **esattamente tre superfici** — la tab `README.md` per intero, le notifiche (titoli e corpo) e i contentDescription; i commenti dei file finti restano inglesi, e la regola è scritta nel commento in testa al `strings.xml` di thabit stesso. Quindi glossare nei commenti sarebbe una divergenza di serie, e in più il canale commenti delle righe test **è già occupato** dal dettaglio vivo (orario del pass, progresso counter, motivo skip, stato quota), che vale più di una glossa ripetuta su ogni riga ogni giorno. Le tre superfici già localizzate sono la risposta: non servono meccanismi nuovi, serve **dichiararle come strato di linguaggio piano e renderle complete**.

**Accolte in VISION:**

1. **Nuovo principio §3.3.7 — "la metafora è un guadagno per chi la riconosce, mai un pedaggio per chi non la riconosce"**, in forma verificabile e non di buona intenzione: **nessun termine CI è mai l'unico posto in cui un fatto esiste**. Un verdetto viaggia sempre con l'aritmetica che lo spiega (`~ build unstable (4/6)`: i numeri *sono* la glossa) e ogni termine che l'app sta mostrando ha un equivalente in lingua piana a un tap di distanza. Da principio, non da buon senso, così le fasi successive possono controllarlo.
2. **La tab `README.md` promossa da pattern di serie a strato di linguaggio piano** (§4.1): era il punto più forte del parere ed è vero — è già prosa localizzata, ed è letteralmente ciò per cui esistono i README. Due vincoli perché non degeneri in manuale: la glossa vive dentro la prosa che c'è già (nessuna sezione glossario) e riguarda **solo i termini a schermo adesso**. Fase 7.
3. **Le notifiche parlano prosa** (§4.4): `daily_commit` è l'unico posto in cui un verdetto compare senza file intorno e senza aritmetica accanto, quindi è l'unico punto in cui la parola nuda escluderebbe davvero. Glifo e numeri restano, la frase è localizzata (pratica di serie: tsteps localizza titoli e corpo). Fase 9.
4. **Le righe parlano parole, non glifi** (§4.1, §4.6): TalkBack non deve mai leggere "parentesi quadra punto parentesi quadra". È accessibilità nel senso strict e insieme la sola sede in cui `[·]` — glifo che nessuno ha mai visto, aggiunto dalla revisione precedente — può essere spiegato. Fasi 3, 10, 13.
5. **La finestra di amend è dichiarata, non scoperta** (§4.2): qui la diagnosi del parere era sull'artefatto sbagliato. `--amend` non è un controllo che l'utente deve riconoscere — il controllo è la riga tappabile, e il problema vero è che **una riga tappabile è invisibile**: nessuno tocca una riga sperando che si apra. Il commit di ieri porta `# still editable until 00:00` (orologio localizzato), che risolve la scopribilità e insieme rende visibile la finestra definita nella revisione precedente. Fase 6.
6. **L'empty state del primo avvio punta alla tab che parla piano** (§4.1): è il momento in cui lo schermo è *solo* metafora, senza ancora una checklist che porti il significato — ed è la prima frase che l'app dice in vita sua. Fase 3.
7. **Le etichette del wizard sono i primi sessanta secondi** (§4.5): tenute al principio §3.3.7 più severamente del resto, ciascuna col suo modo piano accanto al token (`pages >= [20]` si legge senza sapere cosa sia un `assert`). Il vocabolario della metafora sta nell'espansione del test e nel file, dove l'utente non è più un nuovo arrivato. Fase 5.
8. **Criterio di successo verificabile** (§9): la release fallisce se chi non ha mai usato git non riesce a fare il giro completo — installare, aggiungere un'abitudine, capire com'è andata la giornata, correggere ieri — senza dover imparare cosa sia un commit.

**Respinte:**

- **Glossa localizzata nel canale commenti** (motivazione sopra: divergenza dai gemelli, canale già occupato, rumore permanente per tutti dal secondo giorno).
- **Modalità principiante o interruttore che rinomina i termini**: il parere la bocciava già e ha ragione — sono due app in una (§3.3.1) e renderebbero la metafora opzionale, quando il punto è che non costa niente a chi la ignora. Registrata in §3.3.7 perché non venga riproposta.
- **Togliere le decorazioni git** (`⎇ main`, `HEAD → 9e31c7a`, `ro`): restano, ma ora con una regola invece che per affetto — sono firma e non informazione, cioè non portano mai un fatto che non sia già detto in chiaro altrove sullo schermo. Se un giorno una di esse diventa l'unico posto in cui un dato vive, viola §3.3.7 e va accompagnata.

**Difetto trovato per strada e corretto**: §3.3.4 recitava `✗ suite unstable (4/6)`, cioè il glifo di *failed* con la parola di *unstable* (che è `~`) — in un documento che ha "the file must not lie" come primo comandamento, ed è esattamente il tipo di riga che finisce copiata in un test.

---

## Fase 0 — Setup progetto

- [x] Repository git con remote https://github.com/fiorenzobrioni/thabit.git, licenza GPL-3.0, `.gitignore` Android/Kotlin (copiato dalla serie)
- [x] Progetto Android (scheletro Compose), package/applicationId `com.callbackdev.thabit`
- [x] Gradle (Kotlin DSL) identico ai gemelli: Gradle 9.1, AGP 8.13, Kotlin 2.2.20, Compose BOM 2025.08, Material 3, minSdk 33, target/compileSdk 36, version catalog `gradle/libs.versions.toml`
- [x] Dipendenze base = quelle di tsteps **meno Health Connect e meno tutto ciò che è rete** (thabit non ha né sensori esterni né INTERNET — punto di identità, VISION §3.1). Room/WorkManager/DataStore/Navigation già nel catalogo, attivate dalle fasi che le usano
- [x] Font **JetBrains Mono** (400/500/600/700) copiati in `res/font`
- [x] Tema portato da tsteps in `ui/theme/` (Color, Theme, Type, Shape, Depth, SyntaxColors): 3 profili Obsidian/Dracula/Monokai, dark-only, rename `TstepsTheme` → `ThabitTheme`
- [x] Keystore debug condiviso committato `keystore/debug.keystore` (alias `thabit-debug`, password `android`) — stessa filosofia di serie: APK debug di CI e macchine diverse si aggiornano senza reinstallare
- [x] CI GitHub Actions `.github/workflows/android-ci.yml` (copiata da tsteps): test e lint **prima** delle build, artifact APK debug + release minificata debug-signed su flag + mapping R8; `release.yml` su tag `v*` pronto per la Fase 13
- [x] Icona launcher: parentesi graffe e tick nello stesso trattamento di serie (fill `#2B4D73` / stroke `#79C0FF`), soggetto centrale = **checkbox `[x]`** (il gesto dell'app); icona status bar monocromatica `ic_stat_thabit` pronta per la fase notifiche
- [x] `MainActivity` minima: splash screen (brand mark), edge-to-edge con barre forzate scure, placeholder `SkeletonScreen` che disegna un `habits.test` statico (allora `habits_test.yaml`, rinominato nella revisione di fine Fase 1) con gutter e colori sintassi — il primo build ha già la faccia di thabit
- [x] Primo unit test (`ThemeProfileTest`) per esercitare la toolchain JVM; `robolectric.properties` (sdk 35, graphics NATIVE) copiato per i test Compose futuri
- [x] Verifica: `testDebugUnitTest` + `assembleDebug` + `lintDebug` + `assembleRelease -PsignReleaseWithDebugKey` verdi in locale
- [x] Primo commit e push (`796e8a4`); CI verde sul remote al **secondo** run (`fd2314d`): il primo è morto su `./gradlew: Permission denied` — la copia dei file da tsteps via `cp` su Windows non porta il bit eseguibile dentro git; fixato con `git update-index --chmod=+x gradlew`. Gotcha registrato per le prossime app della serie

## Fase 1 — Editor kit (import da tsteps)

I componenti riusabili sono già a tema e già testati: si importano, non si reinventano. Adattare package e ripulire dai riferimenti di dominio.

- [x] `CodeBlockContainer`, `CodeCanvas` (gutter, indent 20px, `EditorOptions`), `EditorTab` + `EditorNavBar`, `TerminalStatusBar`, `TerminalInput`, `GlowFab` (glifo di default **`+`** con contentDescription "Add" — l'unico verbo col glow di thabit è aggiungere un test alla suite); preview ripulite dai riferimenti al dominio passi
- [x] `JsonSyntax` + `MarkdownSyntax` importati (settings.config e stats.md/README li usano)
- [x] **`YamlSyntax` nuovo**: `CheckboxState` (`[x]` diff-verde / `[ ]` **neutro: nessuno span, eredita l'on-surface del canvas** / `[~]` grigio commento / `[!]` diff-rosso), `checkboxToken` nudo per le righe widget future, `yamlTestLine` (`- ` grigio, nome del test senza span — è dato utente, emoji incluse — e commento `#` in coda dimmed come gli hint dei gemelli), `yamlStringLine`/`yamlNumberLine` per le spec espanse. Il canale commenti veste la sintassi del file ospite (VISION §1.1). 10 test dedicati (`YamlSyntaxTest`) sul modello di `JsonSyntaxTest`
- [x] **Revisione VISION, ago 2026 — due deviazioni sul kit già chiuso** (motivazioni per intero nell'indice in testa al file): **(a)** `CheckboxState` passa a cinque stati con `Holding` = `[·]`, grigio commento come `[~]` (entrambi vogliono dire "qui non ti si chiede niente", il glifo fa la differenza) — serve perché sul widget non c'è canale commenti e `[ ] no sugar` leggerebbe come qualcosa ancora da fare; 2 test in più, preview e sample aggiornati con una riga `[·]` e una `[!]` distinte. **(b)** Rename `habits_test.yaml` → **`habits.test`** in kdoc, preview e sample della shell: `- [x] nome` non è YAML valido e il nome del file non può dichiarare una grammatica che il contenuto non rispetta. Il tokenizer resta `YamlSyntax` — la sintassi *è* YAML-flavored, è il file che non promette di esserlo
- [x] `MarkdownTable` importato ora (puro, serve le Fasi 7-8); **`UnitFormat` NON importato — deviazione registrata**: è dominio passi (km/mi, velocità, pace) quasi per intero; thabit scriverà la propria formattazione numeri/percentuali/orari nella fase che ne avrà bisogno — non si copia ciò che non serve
- [x] Test Compose importati e adattati (37 totali, tutti verdi); nav bar con route pulite `editor`/`log`/`stats`/`settings`. **Glifo dell'editor: `checklist`** — il file principale è una lista di checkbox, il `{}` dei gemelli prometterebbe JSON; Log/Stats/Settings mantengono `commit`/`insights`/`code` di serie. Label IT: "Stats" resta inglese (decisione tsteps ereditata: "Statistiche" non sta nella label-sm)
- [x] Shell `ThabitApp` provvisoria: bottom bar + placeholder onesti per tab (`# <file> — not yet written`, `//` per settings.config — regola del file ospite), senza Navigation Compose (il NavHost per-tab è della Fase 4). **Deviazione migliorativa registrata**: la tab editor non è un placeholder ma la suite statica di esempio renderizzata con CodeCanvas+YamlSyntax — esercita il tokenizer nuovo su device da subito; lo `SkeletonScreen` disegnato a mano della Fase 0 è superato e rimosso

- [x] **Revisione accessibilità, ago 2026 — il kit non sapeva parlare** (motivazioni nell'indice in testa al file). Due difetti veri trovati nel codice già scritto, non rimandabili alla Fase 3 perché sono del kit: **(a)** `CodeLine` disegnava la sua `AnnotatedString` con `Text(...)` e aveva solo `onClickLabel` (etichetta dell'*azione*), quindi TalkBack leggeva la riga glifo per glifo (`- [·] no sugar  # holds` letto parentesi per parentesi) — aggiunto `contentDescription`, applicato con `semantics { }` **dopo** il `clickable` così le parole vincono sui glifi *e* il tap sopravvive (con `clearAndSetSemantics` si sarebbe perso il click); `yamlTestLine` lo inoltra, i builder chiave/valore no (`when: daily` si legge già bene: non si aggiunge API speculativa). **(b)** `GlowFab` aveva `contentDescription: String? = "Add"`, letterale inglese in un parametro di accessibilità — ora il default è `stringResource(R.string.cd_add_test)` = "Aggiungi un test alla suite", che dice anche *cosa* fa invece di nominare il glifo
- [x] **`CheckboxState.spokenRes`**: la mappa glifo→parole vive accanto al glifo, non sparsa nelle schermate (`passato` / `ancora da fare` / `sta reggendo, fallisce solo se lo interrompi` / `saltato` / `fallito`); le righe la comporranno col dettaglio vivo ("passato, 07:12"). È anche la sola sede in cui `[·]` viene spiegato a chi non l'ha mai visto. 6 stringhe `cd_*` nuove in `values` e `values-it`
- [x] 5 test nuovi (44 totali, verdi): riga che parla parole e resta tappabile, stati con stringa propria e distinta, traduzioni IT verificate con Robolectric `@Config(qualifiers = "it")`, FAB che nomina il suo verbo in italiano (regressione: prima diceva "Add")

## Fase 2 — Dominio: suite, run, verdetti

Il cuore. Nessuna UI: tutto puro e testabile su JVM.

- [ ] Modello `Habit` (nome, tipo `boolean|counter|avoid`, assert `target/unit/step`, schedule, reminder, emoji, posizione, `createdAt`/`archivedAt`) e `Check` (habitId, data locale, stato `pass|fail|skip`, valore, nota, orario). Room `ThabitDatabase` v1: tabelle `habit`, `check` (una riga per test per giorno, scritta dai tap, con `until` opzionale sugli skip a finestra) e `day` (presenza, sotto) — **i verdetti non si persistono mai** (VISION §6.8: calcolo in lettura, niente stato da corrompere, `--amend` gratis)
- [ ] **Presenza e `no run`** (VISION §3.3.8, §7): riga `day` (data logica, `first_seen`) scritta dalla prima interazione **deliberata** della giornata — apertura app, tap sul widget, tap su azione di notifica. Mai da un evento automatico: un ridisegno del widget o il worker di rollover che timbrano presenza inventerebbero un utente che non c'era, e sarebbe anche il worker a scrivere, cosa che §6.8 gli vieta. La presenza è **prova, non verdetto**: il confine continua a non scrivere niente. Un giorno con test dovuti ma senza riga `day` è `no run` — nessun commit, cella vuota, fuori da ogni denominatore, health neutra, **streak interrotta**. Test: rientro dopo N giorni offline (zero build rosse inventate), presenza scritta una volta sola al giorno, tap a cavallo di `day_ends` attribuito alla data logica giusta
- [ ] **`Schedule` puro**: `daily`, giorni della settimana (`mon,wed,fri`), quota `n/week` (ISO), intervallo `every Nd` (ancorato a `createdAt`); `isDue(date)` e `dueCount(window)` con test su bordi settimana, anni bisestili, cambio schedule a metà settimana (la quota si valuta sullo schedule vigente a fine settimana — registrare). **La quota `n/week` si prototipa per prima**, prima degli altri verdetti: è l'unica voce dell'MVP che tocca tutto (verdetto giornaliero, denominatori, streak, health, intensità heatmap, separatore di settimana) e se sfugge raddoppia la fase — mitigazione scelta al posto di rimandarla, perché senza quote i separatori di settimana perdono la loro riga CI
- [ ] **`DayBoundary`**: il giorno logico con `day_ends` configurabile (default 00:00) — mapping istante→data logica, test su DST 23h/25h (Europe/Rome), cambio timezone, cambio di `day_ends` a giornata in corso (i check già scritti restano sulla loro data logica: il passato non si rietichetta)
- [ ] **`Verdicts` puro**: stato del run di un giorno (per-test: pass/fail/skip/pending; un pending di un giorno chiuso legge fail per definizione **solo se quel giorno è girato**) e build result con semantica Jenkins — `PASSED` (tutti i dovuti passati), `UNSTABLE` (misto), `FAILED` (nessun passato), `NOT_SCHEDULED` (niente in scadenza), `NO_RUN` (giorno mai visto dall'app: nessuna riga `day`, quindi **nessun verdetto affatto** — una build che non è mai partita non è una build fallita); skip fuori dal denominatore. I due esiti senza verdetto restano distinti: "non c'era niente da fare" e "non c'era nessuno" sono fatti diversi e il file non li confonde
- [ ] **`Streaks` puro per test**: passaggi consecutivi sulle sole occorrenze dovute, skip neutri, calcolato in lettura. **Un giorno `no run` rompe la catena** — asimmetria voluta con la health (VISION §3.3.3: streak severa, health indulgente): trattarlo come uno skip renderebbe la streak immortale per chi chiude l'app nei giorni difficili, che è un gaming peggiore di una coverage bassa
- [ ] **`Health` puro** (EMA alla Loop): pass rafforza, fail decade, skip e giorni `no run` neutri; half-life fissata qui e **documentata in VISION §7 e nell'export** (niente numeri segreti: l'utente deve poter ricalcolare tutto dal proprio export)
- [ ] **`Coverage` puro**: giorni dovuti effettivamente girati su giorni dovuti, per finestra — il contrappeso onesto a `no run` (chiudere l'app ferma il rosso, ma la copertura lo dice a chiare lettere)
- [ ] **`Regressions` puro**: un test *era* solido e si sta rompendo — distinto dai flaky, che non lo sono mai stati. Regola di rilevazione fissata qui accanto alla half-life e documentata nell'export come tutto il resto; riportata una volta, senza consigli
- [ ] **Finestre ancorate a `createdAt`**: ogni tasso a 7/30 giorni parte dalla creazione del test — un'abitudine aggiunta ieri è `1/1`, non `1/30`. Un finto 3% di pass rate al secondo giorno è esattamente la bugia che §1.1 vieta
- [ ] `Records` puri: `longest-streak` (test + giorni), `perfect-week` (ISO week al 100% **e con tutti i giorni dovuti effettivamente girati** — senza il vincolo di copertura una settimana con due soli giorni visti sarebbe "100%": un record si guadagna, non lo si ottiene per assenza); `FlakyTests` (pass rate 30d sotto soglia, con soglia-minima-dati)
- [ ] `CommitHash` (SHA-1 di "thabit:<data>" troncato a 7 hex, pattern tsteps), `SettingsStore` minimale (day_ends, week_start, tema, editor), `ServiceLocator` a mano con `overrideForTests`
- [ ] **`AmendWindow` pura**: scrivibili oggi e il giorno logico precedente; la finestra si chiude al `day_ends` di oggi, quindi la grazia si consuma da una giornata piena a zero col passare della giornata. Al rientro dopo più giorni si amenda **solo l'ultimo giorno chiuso**: quelli prima sono `no run` e restano tali (non sono giorni da correggere, sono giorni che non ci sono stati). Test sui bordi: `day_ends` non a mezzanotte, DST dentro la finestra, edit al secondo esatto della chiusura
- [ ] **Rollover worker**: allo scoccare di `day_ends` notifica `daily_commit` e ridisegna il widget — **non scrive mai dati** (né verdetti né presenza) (il commit è una definizione, non una scrittura); safety-net all'apertura app; test DST/timezone/edit di day_ends
- [ ] Test completi su JVM per tutto il sopra

## Fase 3 — Schermata principale (`habits.test`)

- [ ] Rendering live con i componenti Fase 1: header `# suite <data> — N passed · M pending · K skipped` + riga `# logical day <data> — ends 03:00` **solo quando la data logica diverge da quella di parete** (all'una di notte il telefono dice domani e la suite è ancora quella di ieri: non dichiararlo sembra un bug), una riga per test dovuto oggi (checkbox + nome + emoji + commento `#` col dettaglio vivo: orario del pass, progresso counter, motivo skip, stato quota)
- [ ] **Tap sul checkbox**: boolean `[ ]`→`[x]` con orario stampato nel commento, tap di nuovo = undo (oggi è working tree); counter → prompt in place (`> pages: _`, `[esc]`), box `[x]` quando l'assert regge; `[+1]` a fine riga per i counter con step piccoli; avoid = **`[·]`** con `# holds — asserts at commit`, tap → `[!]` failed con nota opzionale (il glifo dedicato serve perché sul widget non c'è commento a disambiguare da un pending)
- [ ] **Espansione in place** al tap sul nome (pattern sessioni tsteps): spec YAML indentata (`when:`, `assert:`, `remind:`), streak corrente, `health: ▓▓░░ NN%`, azioni testo `[~ skip]` (con prompt nota opzionale **e `until:` opzionale**, così la settimana fuori casa è un'interazione e non uno skip per test per giorno — la risposta piena è il branch `vacation`, differito in VISION §6.10), `[edit]`, `[rm]` (archivio, two-tap)
- [ ] Test non dovuti oggi collassati in un commento con `[show]`; quota ancora aperte visibili con `# 2/3 this week`
- [ ] Empty state onesto (`# no tests in the suite yet`) con hint verso il FAB **e verso la tab README** (`# the README tab says what a test is here`): al primo avvio lo schermo è solo metafora, senza ancora una checklist che porti il significato, ed è la prima frase che l'app dice in vita sua (§3.3.7); FAB `+` col glow che apre il wizard (Fase 5 — fino ad allora, commento transiente "coming soon" come fece tsteps col suo FAB)
- [ ] **contentDescription a parole, non a glifi**: ogni riga si annuncia come frase localizzata (*passato alle 07:12* / *ancora da fare* / *sta reggendo, fallisce solo se lo interrompi* / *saltato, giorno di riposo* / *fallito*), perché TalkBack non deve mai leggere "parentesi quadra punto parentesi quadra" — ed è la sola sede in cui `[·]` viene spiegato a chi non l'ha mai visto (pattern `cd_*` di tsteps, che localizza i contentDescription per intero)
- [ ] Status bar: `⎇ main | N tests` a sinistra, `X/N passed` vivo a destra — decorazioni git come firma e non come informazione: non portano mai un fatto che non sia già in chiaro altrove sullo schermo
- [ ] Test: rendering riga per riga, transizioni di stato, undo, counter con assert, avoid, prompt, espansione, empty state

## Fase 4 — Navigazione e Impostazioni (`settings.config`)

- [ ] Navigation Compose con le 4 route (pattern di serie: stack per-tab con `saveState`/`restoreState`)
- [ ] `settings.config` formato serie (JSON con commenti `//`): sezioni `suite` (`day_ends` a cicli o prompt orario, `week_starts`), `editor` (line_numbers/word_wrap → `LocalEditorOptions`), `theme` (profili con `// active`), `notifications` (placeholder onesto fino alla Fase 9), `about`
- [ ] `// Last modified:` alla prima modifica; `$ git restore settings.config` two-tap — **non tocca la suite né i check**: il reset della config non deve mai perdere un dato dell'utente
- [ ] Tema runtime; test su flip, cicli, prompt, reset, day_ends che rietichetta solo il futuro

## Fase 5 — Il wizard (`$ thabit add` / `edit` / `[rm]`)

La suite si costruisce qui: è la fase che rende l'app usabile davvero.

- [ ] **Wizard come sessione terminale** (VISION §4.5): transcript che chiede una cosa alla volta — `> name:` (prompt), `> type:` (token che cicla `[boolean] counter avoid`), `> assert:` (solo counter: target+unit), `> when:` (cicla i 4 schemi + prompt per il custom), `> remind:` (off/orario), `> emoji:` (opzionale, `[skip]`) — chiusura `✓ test added to the suite`. Riusa `TerminalInput` e i token ciclanti di settings: macchinario esistente, composto in conversazione. **Tutto tranne `name:` ha un default** (`boolean`, `daily`, nessun reminder, nessuna emoji): dopo il nome, `[done]` aggiunge il test così com'è e `[more]` prosegue coi prompt rimanenti — sei prompt obbligatori sono diciotto risposte in un minuto e renderebbero falso il criterio di successo di VISION §9
- [ ] **Etichette del wizard = i primi sessanta secondi** (§3.3.7, tenuto più severamente che altrove): ogni passo mostra il suo modo piano accanto al token che chiede (`pages >= [20]` si legge senza sapere cosa sia un `assert`; `[boolean]` cicla con il suo *sì/no* in prosa accanto). Il vocabolario della metafora resta nell'espansione del test e nel file, dove l'utente non è più un nuovo arrivato — e il default + `[done]` dopo il nome riducono comunque l'esposizione a un prompt solo
- [ ] `[edit]` riapre il transcript prefillato; cambi di schedule/assert valgono da oggi (la storia resta con le regole del suo tempo — registrare)
- [ ] `[rm]` two-tap = **archivio, mai delete**: `archivedAt` valorizzato, il test esce dalla suite come riga `-` nel diff di oggi, i run passati restano in ogni giorno committato e nelle statistiche storiche
- [ ] Ordinamento: i test nuovi in coda; riordino manuale rimandato (VISION lo tiene minimale) — se servirà, sarà un verbo testuale, non drag-and-drop
- [ ] Test: wizard end-to-end per i tre tipi, validazioni, edit prefillato, archivio con storia intatta

## Fase 6 — Storico (`habits_history.diff`)

- [ ] Oggi in testa come `# On branch main / # Changes not yet committed (today)` col riassunto del run; un commit per giorno: hash stabile, `Date:` localizzata, messaggio `suite: N/M passed · K skipped`, build result con semantica Jenkins e colori (`✓` verde / `~ unstable` arancio / `✗` rosso), assente nei giorni senza test dovuti **e nei giorni `no run`** (nessun commit affatto: come Jenkins non colora di rosso i giorni in cui nessuno ha pushato, e la storia non guadagna righe che l'utente non ha scritto)
- [ ] Espansione al tap: una riga per test — `+` verde (pass, con orario/valore), `-` rossa (fail), `~` grigia (skip con nota); i cambi di suite come righe di diff (`+ test added: "journal"` / `- test archived: …`)
- [ ] **Separatori settimana ISO** con pass rate e delta (`--- week 34 · 89% passed (+6% vs week 33) ---`) e **lì le verdette quota**: `quota: run 5k 2/3 ✗` — una quota non fallisce mai un giorno, fallisce (o passa) la settimana; una settimana senza nessun giorno girato non avanza pretese di quota
- [ ] **`--amend`**: il run di ieri espanso resta interattivo (stessi tap di oggi) finché la finestra è aperta (`AmendWindow`, Fase 2: si chiude al `day_ends` di oggi); il commit di ieri porta `# still editable until 00:00` (orologio localizzato) — **la finestra si dichiara, non si scopre**: una riga tappabile è invisibile e nessuno tocca una riga sperando che si apra, quindi il meccanismo migliore dell'app resterebbe inutilizzato (`--amend` non è un controllo da riconoscere: il controllo è la riga); al primo edit il commit guadagna il marcatore permanente `# amended`; da due giorni indietro è storia read-only per sempre, e al rientro da più giorni offline è amendabile solo l'ultimo giorno chiuso
- [ ] Tag sui record (`tag: perfect-week`, `tag: longest-streak`) in arancio; status bar `⎇ main | N commits` / `HEAD → <hash>`; empty state onesto
- [ ] Test: documento riga per riga, tre verdetti, espansioni, quota su settimana, amend con marcatore, immutabilità oltre ieri, tag

## Fase 7 — README.md del giorno (seconda tab dell'editor)

- [ ] `WorkspaceStore` portato (DataStore `workspace`, tab persistita, scroll separati)
- [ ] Prosa **completamente localizzata**: titolo = data estesa; `## Oggi` (cosa è passato e cosa resta, parole neutre); `## Stato` (build badge del giorno, streak degne di nota, health in parole solo se dice qualcosa); `## Settimana` (tabella 7 giorni via `MarkdownTable`, oggi in grassetto e vivo, giorni senza test dovuti come `—`); footer `*Calcolato sul dispositivo · N giorni committati*`
- [ ] **Questa tab è lo strato di linguaggio piano dell'app** (VISION §3.3.7, §4.1), non un vezzo di serie: qualunque termine gli altri file stiano mostrando adesso, qui è detto in una frase nella lingua del lettore — `~ build unstable` diventa "quattro test su sei passati", un flaky diventa un'abitudine che salti spesso, `[·]` diventa "qui non devi fare niente, a meno che tu non lo interrompa". Due vincoli perché non degeneri in manuale: la glossa vive **dentro la prosa che c'è già** (nessuna sezione glossario) e riguarda **solo i termini a schermo adesso**
- [ ] Test: localizzazione, omissioni oneste, tabella rettangolare, oggi vivo, **copertura del vocabolario**: per ogni termine CI che le altre schermate possono mostrare (`unstable`, `flaky`, `coverage`, `regression`, `no run`, `holds`, `amended`), la prosa di questa tab lo dice in parole quando quel termine è a schermo — il controllo verificabile del principio §3.3.7

## Fase 8 — Stats (`stats.md`)

- [ ] **Heatmap contributi 12 settimane** (riuso del dominio `Heatmap` di tsteps): intensità = frazione della suite dovuta passata quel giorno, bucket relativi ai quartili dell'utente, skip fuori denominatore, futuri vuoti, **giorni `no run` vuoti come i futuri** (non è un fallimento: è un giorno che non sappiamo), oggi vivo
- [ ] **`## coverage`**: `24 of 30 days ran · 6 days no run` con barra, e il commento che spiega il concetto una volta (`# a day with no run is not a failed build — it is a build that never started`). È la sezione che rende letterale il titolo del file (§4.3 si chiamava *coverage report* senza avere una coverage) e il contrappeso onesto a `no run`
- [ ] Tabella `## suite health`: per test — health %, streak corrente, pass 30d; ordinata per health discendente
- [ ] `## flaky tests`: pass rate 30d sotto soglia, col numero e un solo hint fisso e fattuale; sezione presente solo con dati sufficienti — mai un dito puntato, solo un report. **Tutte le finestre partono da `createdAt`**: un test aggiunto ieri non compare fra i flaky con un finto 3%
- [ ] `## regressions`: un test che *era* solido e si sta rompendo (`meditate 10 min — 43 days green, 3 of the last 5 red`), riportato una volta e senza consigli — distinto dai flaky, che non sono mai stati solidi
- [ ] `## tags`: `longest-streak`, `perfect-week` — righe cliccabili verso il commit nel Log (pattern `LogFocus`)
- [ ] Rendering markdown evidenziato, heatmap colorata a mano, footer onesto, status bar `⎇ main | ro`; empty state con griglia comunque visibile. I titoli di sezione e i commenti-hint restano inglesi (regola di serie, `strings.xml`): la lingua piana per `flaky`/`coverage`/`regression` la porta la tab README (Fase 7), e ogni numero mostrato qui viaggia con la sua frazione, che è la glossa che non ha bisogno di traduzione
- [ ] Test: geometria heatmap con denominatore-dovuti, tabelle, flaky con soglia dati, tag cliccabili, empty state

## Fase 9 — Notifiche

- [ ] Permission `POST_NOTIFICATIONS` runtime col pattern completo di serie nella sezione `notifications` (toggle gated, status line, detour impostazioni di sistema)
- [ ] **Reminder per test** (`remind:` dal wizard): sveglie **inesatte** via `AlarmManager.setWindow` (~10 min, niente permission exact-alarm — VISION §6.7: un reminder è un nudge, non una sveglia; dichiarato nel file), riarmate al boot e a ogni edit; notifica con **azione `pass`** per i boolean — il test si smarca dalla tendina (e il tap sull'azione scrive la presenza del giorno, Fase 2); i counter aprono l'app sul prompt. **Un solo alarm "prossimo reminder" riarmato è stato valutato e respinto** (motivazione in testa al file e in VISION §7): gli alarm inesatti sono batchati dall'OS, quindi non si risparmia batteria, e una catena che si riarma perde tutti i reminder su un fire mancato
- [ ] `daily_commit` (default on, canale LOW silenzioso): il build result del giorno chiuso, compatta una riga / espansa stile `git log` (pattern due-forme di tsteps). **Prosa localizzata, mai il token nudo**: è l'unico posto in cui un verdetto compare senza file intorno e senza aritmetica accanto, quindi `~` e i numeri restano ma la frase è nella lingua del lettore (*quattro test su sei passati*), non `build unstable` (pattern `notif_title_*` di tsteps, che localizza titoli e corpo)
- [ ] `pending_digest` (**default off, opt-in** — VISION §3.3.4): un solo riepilogo serale all'ora configurata (`✗ 3 tests still pending`), mai un nag per test
- [ ] Canali separati, tutte disattivabili, linguaggio neutro sempre; niente notifiche motivazionali, mai
- [ ] Test: contenuti puri delle due forme, azione pass end-to-end, digest solo opt-in, riarmo sveglie

## Fase 10 — Widget (`thabit --status`)

Architettura widget di serie ereditata intera (sizes-map, un gradino per riga, breakpoint misurati, `monospace` di sistema, opacità configurabile, zero polling).

- [ ] Content builder puro: prompt `$ thabit --status 🧪`, barra suite `▓░ N/M`, i test dovuti oggi in colonne compatte `[x] nome` (avoid con `[·]`: qui il glifo è l'unico canale, non c'è commento a disambiguare da un pending), commento `# N pending — tap to pass`; tier che tagliano dal fondo, il minimo è la sola barra suite
- [ ] **Check-off dal widget**: le righe boolean portano PendingIntent propri (action+requestCode distinti — lezione filterEquals di tsteps) e smarcano senza aprire l'app; counter/avoid aprono l'app sul punto giusto
- [ ] Ridisegni su: tap, rollover, modifiche suite/settings; il widget che non ha ancora ridisegnato mostra la data che sta renderizzando — mai la suite di ieri spacciata per oggi. **Un ridisegno non è presenza**: solo il tap scrive la riga `day` (Fase 2); un widget guardato e mai toccato non testimonia niente, e il rollover che timbrasse presenza da solo trasformerebbe ogni giorno a telefono acceso in un giorno "girato"
- [ ] contentDescription delle righe a parole e non a glifi, come nell'app (Fase 3): sul widget è ancora più necessario, perché qui non c'è nemmeno il commento a dare contesto
- [ ] Test: transcript e budget per tier, binary search sui gradini (pattern tsteps), colori via span attraverso l'IPC, intent per riga

## Fase 11 — Export dati

- [ ] `$ thabit export --json` e `--csv` in fondo a settings.config (pattern tsteps Fase 13 per intero: MediaStore pending→publish, zero permission storage, nome riportato = quello vero, esiti `// wrote …` / `// nothing to export yet` / `// ERROR:`)
- [ ] JSON un documento (suite + tutti i check + **tutte le righe `day` di presenza**, un record per riga); CSV tre file (`thabit-suite-*.csv` + `thabit-checks-*.csv` + `thabit-days-*.csv`); `Locale.ROOT`, valori canonici, half-life dell'health e regola delle regressioni dichiarate nell'header — tutto ricalcolabile, coverage e `no run` compresi (una statistica che l'utente non può verificare è una formula segreta)
- [ ] Onestà del formato: skip con nota, `# amended` come flag dato, test archiviati inclusi con `archived_at` (la storia è dell'utente)
- [ ] Formato documentato nel README (sezione Export); test su documenti e sink

## Fase 12 — Rifiniture dal campo

Fase cuscinetto deliberata: la serie ha imparato che il device del committente riscrive sempre qualcosa (status bar affamate, controlli che non si spiegano, notifiche da ridisegnare). Qui si raccolgono le tornate di feedback prima del restyling finale, ciascuna registrata con la motivazione.

- [ ] Tornata/e di feedback su device del committente
- [ ] Valutare qui, con dati d'uso reali: **il runner guidato `$ thabit run` con il FAB che diventa `▶`** (VISION §6.9 — la domanda da rispondere sul device: passare in rassegna i pendenti aggiunge qualcosa a spuntarli nel file, o è una terza superficie per gli stessi check?), **i profili di suite come branch** (§6.10 — dopo che il modello della suite ha dimostrato di essere stabile), gruppi per momento della giornata (sezioni YAML), riordino manuale, `$ thabit watch` (timer come processo) — si aprono solo se la pratica li chiede (VISION §6)

## Fase 13 — Restyling pre-v1 e Release

- [ ] Passata di coerenza design su tutte le schermate: vincoli, spazi, scala font di sistema, TalkBack (le righe si annunciano a parole, mai a glifi)
- [ ] **Audit del principio §3.3.7 schermata per schermata**: nessun termine CI è l'unico posto in cui un fatto esiste — ogni verdetto ha la sua aritmetica accanto, ogni termine a schermo ha la sua frase nella tab README, nessuna decorazione git porta un dato che non sia in chiaro altrove. È il controllo che rende l'accessibilità una verifica invece di una buona intenzione
- [ ] Edge case checklist su device: DST e cambio timezone, cambio `day_ends` a giornata in corso, riavvio (riarmo sveglie), giorno vuoto, suite vuota, amend al limite della finestra, archivio con storia lunga, battery saver
- [x] Keystore di release reale — **anticipato a prima della Fase 1** (ago 2026, in coda al giro che ha messo in riga tutte le app della serie): chiave RSA 4096 trentennale (alias `thabit`) generata dal committente con `keytool`, fuori repo in `C:\Fiorenzo\keys\` con backup nel password manager; 4 proprietà `THABIT_*` compilate in `~/.gradle/gradle.properties`, 4 GitHub Secrets caricati, `release.yml` su tag `v*` già nel repo dalla Fase 0. Verificato in locale: `assembleRelease` produce `app-release.apk` firmato, certificato controllato con apksigner (`CN=callbackdev`, fingerprint distinto dalle altre 4 app); il percorso `-PsignReleaseWithDebugKey` è quello già esercitato dalla CI verde. Il keystore di debug committato è nuovo e unico (alias `thabit-debug`, fingerprint diverso dai gemelli)
- [ ] Versione 1.0.0, `CHANGELOG.md`, screenshot reali in `docs/screenshots/`, README definitivo, tag e prima GitHub Release verificata

---

## Note trasversali

- **Vincoli di design non negoziabili** (vedi `CLAUDE.md` e VISION §1.2): solo JetBrains Mono (eccetto widget), griglia 4px, indent 20px, niente ombre (bordi 1px + glow del FAB), raggio 4px ovunque, controlli renderizzati come testo, emoji come icone nel testo.
- **Regola l10n**: il "codice" resta inglese (chiavi YAML/JSON, filenames, commenti, output terminale, hash, check line); chrome e valori-dato localizzati IT/EN; i nomi dei test sono dati dell'utente. `README.md` (la tab) è prosa: localizzata per intero.
- **Niente rete**: se una feature futura chiede la permission INTERNET, non è una feature di thabit.
- **Verdetti mai persistiti**: build result, streak, health e record si calcolano in lettura dai check. Il worker di mezzanotte notifica e ridisegna, non scrive.
- **L'unica altra scrittura è la presenza**: la riga `day` della prima interazione deliberata della giornata. Non è un verdetto ma una prova, ed è ciò che rende dicibile `no run` — un giorno che l'app non ha visto è ignoto, non fallito: niente commit, cella vuota, fuori da ogni denominatore, health neutra, streak interrotta.
- **La metafora non è un pedaggio** (VISION §3.3.7): nessun termine CI è l'unico posto in cui un fatto esiste — i verdetti viaggiano con la loro aritmetica, e la lingua piana vive nelle tre superfici localizzate della serie (tab README, notifiche, contentDescription). I commenti dei file finti restano inglesi: è per questo che quelle tre superfici devono essere complete.
- **Il file principale è `habits.test`**, non `.yaml`: la sintassi è YAML-flavored, il nome non promette una grammatica che `- [x] nome` non rispetta.
- **Ordine**: Fasi 1–2 sono il fondamento (la 2 in parallelo alla 1). La 3 dipende da 1–2; la 5 rende l'app usabile e sblocca il valore di 6–10; il widget (10) dopo che dominio e settings sono stabili.
- **Import dalla serie**: i componenti si copiano adattando il package, mai linkando i repo; ogni divergenza che emerge (bug fixati qui, migliorie) va valutata per il backport a tweather/tsteps.
- Aggiornare questo file smarcando i passi completati e annotando le deviazioni dalla VISION con la motivazione.
