# PLANNING.md — Piano di realizzazione thabit

Piano di sviluppo per **thabit**, habit tracker Android (Kotlin + Jetpack Compose) con UI in stile code editor + CI/test suite (tema "Obsidian Syntax", ereditato dalla serie). Ogni passo è smarcabile: `[ ]` da fare → `[x]` completato.

Riferimenti: `VISION.md` (identità, metafora CI, schermate, metriche, decisioni prese studiando il campo — Loop, Streaks, Habitify, HabitNow, Habitica), i repo gemelli [tweather](https://github.com/fiorenzobrioni/tweather) e [tsteps](https://github.com/fiorenzobrioni/tsteps) come sorgente dei componenti UI e delle decisioni già validate su device (design system in `obsidian_syntax/DESIGN.md` di tweather; il porting più recente dei componenti è quello di tsteps, che è quindi la sorgente preferita per la copia).

Regola del piano (di serie): ogni decisione e ogni deviazione dalla vision va registrata qui **con la motivazione**, nella fase in cui è stata presa.

### Revisione della VISION — agosto 2026 (fine Fase 1)

Giro di critica alla VISION a Fase 1 chiusa. Le decisioni sono distribuite nelle fasi che le implementano; qui l'indice con il perché, perché è il tipo di ragionamento che si perde per strada.

**Accolte in VISION:**

1. **`no run` — l'app può dire "non lo so"** (§3.3.8, §7, §4.3): senza una riga di presenza, l'assenza di check significa insieme "ho fallito" e "il telefono era spento", e con i verdetti calcolati in lettura il rientro da nove giorni offline produce nove `✗ build failed` di dati inventati — la bugia che §1.1 vieta. Una build che non è mai partita non è una build fallita: Jenkins non colora di rosso i giorni in cui nessuno ha pushato. Fase 2 (dominio), 8 (coverage), 6 (nessun commit).
2. **Presenza scritta solo da interazioni deliberate** (§7): la proposta originale includeva il render del widget, che però si ridisegna da solo al rollover — la presenza sarebbe stata automatica ogni giorno a telefono acceso, e l'avrebbe scritta il worker, che per contratto §6.8 non scrive mai. Correzione: apertura app, tap sul widget, tap su azione di notifica. Fase 2, 10.
3. **`no run` rompe la streak, non la health** (§3.3.8): la simmetria con lo skip era la scelta ovvia ma regalava il gaming peggiore — chiudere l'app nei giorni difficili per non perdere mai la catena. Asimmetria voluta e coerente con §3.3.3: streak severa (catena di pass digitati), health indulgente. Fase 2.
4. **`perfect-week` richiede copertura piena** (§5): con i `no run` fuori dai denominatori, una settimana con due soli giorni girati e passati sarebbe stata "100%". Un record va guadagnato, non ottenuto per assenza. Fase 2, 8.
5. **`habits.test` al posto di `habits_test.yaml`** (§4.1): `- [x] meditate 10 min` non è YAML valido (uno scalare plain non può seguire una flow sequence), e in una serie il cui primo comandamento è "the file must not lie" un file che dichiara una grammatica e non la rispetta era l'unico punto in cui la serie veniva meno a sé stessa. `.test` è il formato del runner, come `.diff` è un formato e non un linguaggio; `#` come canale commenti resta giustificato. `settings.config` invece era già a posto (JSON con `//` è JSONC, formato reale, precedente di tsteps). Fase 1 (rename), 3.
6. **`[·]` per gli avoid test** (§4.1, §4.6): `[ ]` significava "pending" per un boolean e "sta reggendo" per un avoid — nel file li distingueva il commento, sul widget nulla. Fase 1 (tokenizer), 3, 10.
7. **Coverage e regressions in `stats.md`** (§4.3): §4.3 si intitolava *coverage report* senza contenere una metrica chiamata coverage — ora ce l'ha, ed è il contrappeso onesto a `no run` (non aprire mai l'app ferma il rosso, ma la barra di copertura lo dichiara). Le regressioni sono distinte dai flaky (flaky = non è mai stato solido; regression = *era* solido e si sta rompendo) e sono la riga più azionabile che l'app possa produrre. Fase 2, 8.
8. **Finestre statistiche ancorate a `createdAt`** (§4.3, §5): un test aggiunto ieri sarebbe apparso come "3% pass rate" fra i flaky il giorno dopo. Fase 2, 8.
9. **Finestra di amend definita** (§3.3.5): "ieri resta editabile" non è una durata. È il giorno logico precedente, editabile fino al `day_ends` di oggi — la grazia si consuma col passare della giornata; al rientro da più giorni si amenda solo l'ultimo giorno chiuso, gli altri sono `no run` e restano tali. Fase 2, 6.
10. **Wizard con default e uscita anticipata** (§4.5): "tre abitudini in meno di un minuto" (§9) con sei prompt obbligatori sono diciotto risposte in sessanta secondi, cioè un criterio di successo falso. Tutto tranne `name:` ha un default, `[done]` chiude subito, `[more]` continua. Fase 5.
11. **Giorno logico dichiarato quando diverge dalla data di parete** (§4.1): con `day_ends: "03:00"`, all'una di notte il telefono dice il 21 e la suite è quella del 20 — non dichiararlo sembra un bug. Fase 3.
12. **Skip con `until:`** (§4.1): la settimana fuori casa è il caso più frequente della categoria dopo `day_ends`. Fase 3, 5.
13. **Export della presenza** (§7): coverage e `no run` devono essere ricalcolabili dall'export come tutto il resto, o sono formule segrete. Fase 11.

**Registrate in VISION come progettate-ma-differite** (§6.9, §6.10): il runner guidato `$ thabit run` con il FAB che passa da `+` a `▶`, e i profili di suite come branch git (`git checkout vacation`) con `⎇` tappabile. La critica al FAB è giusta e sta agli atti — `+` è un verbo di setup, mentre i gemelli hanno il glow su un verbo quotidiano — ma il file principale **è già** il runner (§3.3.2: un colpo d'occhio, un tap) e il widget copre già i boolean: una terza superficie per gli stessi check va chiesta dall'uso reale, non dalla simmetria dei verbi — la regola con cui la stessa §6.2 ha parcheggiato il timer. Valutazione in Fase 12 con dati d'uso. Liberato il nome, il timer per-test diventa `$ thabit watch` (watch mode), che è anche il nome giusto.

**Respinte, con motivazione registrata:**

- **Un solo alarm riarmato al posto dei reminder per-test** (proposto come coerenza con "one shared job"): gli alarm inesatti sono batchati dall'OS, quindi i *fire* sono identici e quindici registrazioni pendenti costano quanto una — il risparmio di batteria non esiste. In compenso una catena che si riarma a ogni fire perde **tutti** i reminder su un fire mancato, mentre alarm indipendenti degradano un test alla volta. La regola di serie riguarda i job periodici di polling, non gli alarm event-driven. Registrata in VISION §7 e in `CLAUDE.md` perché non venga riproposta. Fase 9.
- **Quota fuori dall'MVP**: il rischio di scope è reale (una quota `3/week` tocca verdetto, denominatori, streak, health, intensità heatmap e separatore di settimana), ma togliere le quote amputerebbe i separatori settimana, che sono identità. Mitigazione scelta: **si prototipa per prima** nel dominio puro della Fase 2, prima di tutto il resto dei verdetti. Fase 2.
- **Sensori che passano i test** (§6.11): non era in discussione, ma va agli atti — tsteps scrive i passi su Health Connect, quindi `it("walks 8000 steps")` potrebbe diventare verde da solo. Il senso di quest'app è che la spunta l'ha digitata una persona.

### Revisione accessibilità ai non-professionisti — agosto 2026

Secondo giro di critica: l'app è usabile da chi non riconosce la metafora CI? La diagnosi accolta è che **estetica e vocabolario sono due cose diverse** — il look da editor (monospaziato, gutter, controlli testuali) non chiede nulla al lettore, è una checklist ben vestita, e il gesto quotidiano è il tap che fanno tutte le app della categoria; è il *vocabolario tecnico* che può escludere, e sono meno di dieci parole. La cura non richiede di annacquare niente, ma **non è quella proposta**.

**La correzione di rotta.** La proposta era glossare le stringhe difficili nel canale commenti (`~ build unstable (4/6)` seguito da `# 4 test su 6 passati`). Verificato sui gemelli prima di decidere: il `values-it` di tsteps ha 65 righe e localizza **esattamente tre superfici** — la tab `README.md` per intero, le notifiche (titoli e corpo) e i contentDescription; i commenti dei file finti restano inglesi, e la regola è scritta nel commento in testa al `strings.xml` di thabit stesso. Quindi glossare nei commenti sarebbe una divergenza di serie, e in più il canale commenti delle righe test **è già occupato** dal dettaglio vivo (orario del pass, progresso counter, motivo skip, stato quota), che vale più di una glossa ripetuta su ogni riga ogni giorno. Le tre superfici già localizzate sono la risposta: non servono meccanismi nuovi, serve **dichiararle come strato di linguaggio piano e renderle complete**.

**Accolte in VISION:**

1. **Nuovo principio §3.3.7 — "la metafora è un guadagno per chi la riconosce, mai un pedaggio per chi non la riconosce"**, in forma verificabile e non di buona intenzione: **nessun termine CI è mai l'unico posto in cui un fatto esiste**. Un verdetto viaggia sempre con l'aritmetica che lo spiega (`~ build unstable (4/6)`: i numeri *sono* la glossa) e ogni termine che l'app sta mostrando ha un equivalente in lingua piana a un tap di distanza. Da principio, non da buon senso, così le fasi successive possono controllarlo.
2. **La tab `README.md` promossa da pattern di serie a strato di linguaggio piano** (§4.1): era il punto più forte del parere ed è vero — è già prosa localizzata, ed è letteralmente ciò per cui esistono i README. Due vincoli perché non degeneri in manuale: la glossa vive dentro la prosa che c'è già (nessuna sezione glossario) e riguarda **solo i termini a schermo adesso**. Fase 7.
3. **Le notifiche parlano prosa** (§4.4): `daily_commit` è l'unico posto in cui un verdetto compare senza file intorno e senza aritmetica accanto, quindi è l'unico punto in cui la parola nuda escluderebbe davvero. Glifo e numeri restano, la frase è localizzata (pratica di serie: tsteps localizza titoli e corpo). Fase 9.
4. **Le righe parlano parole, non glifi** (§4.1, §4.6): TalkBack non deve mai leggere "parentesi quadra punto parentesi quadra". È accessibilità nel senso strict e insieme la sola sede in cui `[·]` — glifo che nessuno ha mai visto, aggiunto dalla revisione precedente — può essere spiegato. Fasi 3, 10, 13.
5. **La finestra di amend è dichiarata, non scoperta** (§4.2): qui la diagnosi del parere era sull'artefatto sbagliato. `--amend` non è un controllo che l'utente deve riconoscere — il controllo è la riga tappabile, e il problema vero è che **una riga tappabile è invisibile**: nessuno tocca una riga sperando che si apra. Il commit di ieri porta `# still editable until 00:00` (orologio localizzato), che risolve la scopribilità e insieme rende visibile la finestra definita nella revisione precedente. Fase 6.
6. **L'empty state del primo avvio punta alla tab che parla piano** (§4.1): è il momento in cui lo schermo è *solo* metafora, senza ancora una checklist che porti il significato — ed è la prima frase che l'app dice in vita sua. Fase 3.
7. **Le etichette del wizard sono i primi sessanta secondi** (§4.5): tenute al principio §3.3.7 più severamente del resto, ciascuna col suo modo piano accanto al token (`pages >= [20]` si legge senza sapere cosa sia un `assert`). Il vocabolario della metafora sta nell'espansione del test e nel file, dove l'utente non è più un nuovo arrivato. Fase 5.
8. **Criterio di successo verificabile** (§9): la release fallisce se chi non ha mai usato git non riesce a fare il giro completo — installare, aggiungere un'abitudine, capire com'è andata la giornata, correggere ieri — senza dover imparare cosa sia un commit.

**Respinte:**

- **Glossa localizzata nel canale commenti** (motivazione sopra: divergenza dai gemelli, canale già occupato, rumore permanente per tutti dal secondo giorno).
- **Modalità principiante o interruttore che rinomina i termini**: il parere la bocciava già e ha ragione — sono due app in una (§3.3.1) e renderebbero la metafora opzionale, quando il punto è che non costa niente a chi la ignora. Registrata in §3.3.7 perché non venga riproposta.
- **Togliere le decorazioni git** (`⎇ main`, `HEAD → 9e31c7a`, `ro`): restano, ma ora con una regola invece che per affetto — sono firma e non informazione, cioè non portano mai un fatto che non sia già detto in chiaro altrove sullo schermo. Se un giorno una di esse diventa l'unico posto in cui un dato vive, viola §3.3.7 e va accompagnata.

**Difetto trovato per strada e corretto**: §3.3.4 recitava `✗ suite unstable (4/6)`, cioè il glifo di *failed* con la parola di *unstable* (che è `~`) — in un documento che ha "the file must not lie" come primo comandamento, ed è esattamente il tipo di riga che finisce copiata in un test.

---

## Fase 0 — Setup progetto

- [x] Repository git con remote https://github.com/fiorenzobrioni/thabit.git, licenza GPL-3.0, `.gitignore` Android/Kotlin (copiato dalla serie)
- [x] Progetto Android (scheletro Compose), package/applicationId `com.callbackdev.thabit`
- [x] Gradle (Kotlin DSL) identico ai gemelli: Gradle 9.1, AGP 8.13, Kotlin 2.2.20, Compose BOM 2025.08, Material 3, minSdk 33, target/compileSdk 36, version catalog `gradle/libs.versions.toml`
- [x] Dipendenze base = quelle di tsteps **meno Health Connect e meno tutto ciò che è rete** (thabit non ha né sensori esterni né INTERNET — punto di identità, VISION §3.1). Room/WorkManager/DataStore/Navigation già nel catalogo, attivate dalle fasi che le usano
- [x] Font **JetBrains Mono** (400/500/600/700) copiati in `res/font`
- [x] Tema portato da tsteps in `ui/theme/` (Color, Theme, Type, Shape, Depth, SyntaxColors): 3 profili Obsidian/Dracula/Monokai, dark-only, rename `TstepsTheme` → `ThabitTheme`
- [x] Keystore debug condiviso committato `keystore/debug.keystore` (alias `thabit-debug`, password `android`) — stessa filosofia di serie: APK debug di CI e macchine diverse si aggiornano senza reinstallare
- [x] CI GitHub Actions `.github/workflows/android-ci.yml` (copiata da tsteps): test e lint **prima** delle build, artifact APK debug + release minificata debug-signed su flag + mapping R8; `release.yml` su tag `v*` pronto per la Fase 13
- [x] Icona launcher: parentesi graffe e tick nello stesso trattamento di serie (fill `#2B4D73` / stroke `#79C0FF`), soggetto centrale = **checkbox `[x]`** (il gesto dell'app); icona status bar monocromatica `ic_stat_thabit` pronta per la fase notifiche
- [x] `MainActivity` minima: splash screen (brand mark), edge-to-edge con barre forzate scure, placeholder `SkeletonScreen` che disegna un `habits.test` statico (allora `habits_test.yaml`, rinominato nella revisione di fine Fase 1) con gutter e colori sintassi — il primo build ha già la faccia di thabit
- [x] Primo unit test (`ThemeProfileTest`) per esercitare la toolchain JVM; `robolectric.properties` (sdk 35, graphics NATIVE) copiato per i test Compose futuri
- [x] Verifica: `testDebugUnitTest` + `assembleDebug` + `lintDebug` + `assembleRelease -PsignReleaseWithDebugKey` verdi in locale
- [x] Primo commit e push (`796e8a4`); CI verde sul remote al **secondo** run (`fd2314d`): il primo è morto su `./gradlew: Permission denied` — la copia dei file da tsteps via `cp` su Windows non porta il bit eseguibile dentro git; fixato con `git update-index --chmod=+x gradlew`. Gotcha registrato per le prossime app della serie

## Fase 1 — Editor kit (import da tsteps)

I componenti riusabili sono già a tema e già testati: si importano, non si reinventano. Adattare package e ripulire dai riferimenti di dominio.

- [x] `CodeBlockContainer`, `CodeCanvas` (gutter, indent 20px, `EditorOptions`), `EditorTab` + `EditorNavBar`, `TerminalStatusBar`, `TerminalInput`, `GlowFab` (glifo di default **`+`** con contentDescription "Add" — l'unico verbo col glow di thabit è aggiungere un test alla suite); preview ripulite dai riferimenti al dominio passi
- [x] `JsonSyntax` + `MarkdownSyntax` importati (settings.config e stats.md/README li usano)
- [x] **`YamlSyntax` nuovo**: `CheckboxState` (`[x]` diff-verde / `[ ]` **neutro: nessuno span, eredita l'on-surface del canvas** / `[~]` grigio commento / `[!]` diff-rosso), `checkboxToken` nudo per le righe widget future, `yamlTestLine` (`- ` grigio, nome del test senza span — è dato utente, emoji incluse — e commento `#` in coda dimmed come gli hint dei gemelli), `yamlStringLine`/`yamlNumberLine` per le spec espanse. Il canale commenti veste la sintassi del file ospite (VISION §1.1). 10 test dedicati (`YamlSyntaxTest`) sul modello di `JsonSyntaxTest`
- [x] **Revisione VISION, ago 2026 — due deviazioni sul kit già chiuso** (motivazioni per intero nell'indice in testa al file): **(a)** `CheckboxState` passa a cinque stati con `Holding` = `[·]`, grigio commento come `[~]` (entrambi vogliono dire "qui non ti si chiede niente", il glifo fa la differenza) — serve perché sul widget non c'è canale commenti e `[ ] no sugar` leggerebbe come qualcosa ancora da fare; 2 test in più, preview e sample aggiornati con una riga `[·]` e una `[!]` distinte. **(b)** Rename `habits_test.yaml` → **`habits.test`** in kdoc, preview e sample della shell: `- [x] nome` non è YAML valido e il nome del file non può dichiarare una grammatica che il contenuto non rispetta. Il tokenizer resta `YamlSyntax` — la sintassi *è* YAML-flavored, è il file che non promette di esserlo
- [x] `MarkdownTable` importato ora (puro, serve le Fasi 7-8); **`UnitFormat` NON importato — deviazione registrata**: è dominio passi (km/mi, velocità, pace) quasi per intero; thabit scriverà la propria formattazione numeri/percentuali/orari nella fase che ne avrà bisogno — non si copia ciò che non serve
- [x] Test Compose importati e adattati (37 totali, tutti verdi); nav bar con route pulite `editor`/`log`/`stats`/`settings`. **Glifo dell'editor: `checklist`** — il file principale è una lista di checkbox, il `{}` dei gemelli prometterebbe JSON; Log/Stats/Settings mantengono `commit`/`insights`/`code` di serie. Label IT: "Stats" resta inglese (decisione tsteps ereditata: "Statistiche" non sta nella label-sm)
- [x] Shell `ThabitApp` provvisoria: bottom bar + placeholder onesti per tab (`# <file> — not yet written`, `//` per settings.config — regola del file ospite), senza Navigation Compose (il NavHost per-tab è della Fase 4). **Deviazione migliorativa registrata**: la tab editor non è un placeholder ma la suite statica di esempio renderizzata con CodeCanvas+YamlSyntax — esercita il tokenizer nuovo su device da subito; lo `SkeletonScreen` disegnato a mano della Fase 0 è superato e rimosso

- [x] **Revisione accessibilità, ago 2026 — il kit non sapeva parlare** (motivazioni nell'indice in testa al file). Due difetti veri trovati nel codice già scritto, non rimandabili alla Fase 3 perché sono del kit: **(a)** `CodeLine` disegnava la sua `AnnotatedString` con `Text(...)` e aveva solo `onClickLabel` (etichetta dell'*azione*), quindi TalkBack leggeva la riga glifo per glifo (`- [·] no sugar  # holds` letto parentesi per parentesi) — aggiunto `contentDescription`, applicato con `semantics { }` **dopo** il `clickable` così le parole vincono sui glifi *e* il tap sopravvive (con `clearAndSetSemantics` si sarebbe perso il click); `yamlTestLine` lo inoltra, i builder chiave/valore no (`when: daily` si legge già bene: non si aggiunge API speculativa). **(b)** `GlowFab` aveva `contentDescription: String? = "Add"`, letterale inglese in un parametro di accessibilità — ora il default è `stringResource(R.string.cd_add_test)` = "Aggiungi un test alla suite", che dice anche *cosa* fa invece di nominare il glifo
- [x] **`CheckboxState.spokenRes`**: la mappa glifo→parole vive accanto al glifo, non sparsa nelle schermate (`passato` / `ancora da fare` / `sta reggendo, fallisce solo se lo interrompi` / `saltato` / `fallito`); le righe la comporranno col dettaglio vivo ("passato, 07:12"). È anche la sola sede in cui `[·]` viene spiegato a chi non l'ha mai visto. 6 stringhe `cd_*` nuove in `values` e `values-it`
- [x] 5 test nuovi (44 totali, verdi): riga che parla parole e resta tappabile, stati con stringa propria e distinta, traduzioni IT verificate con Robolectric `@Config(qualifiers = "it")`, FAB che nomina il suo verbo in italiano (regressione: prima diceva "Add")

## Fase 2 — Dominio: suite, run, verdetti

Il cuore. Nessuna UI: tutto puro e testabile su JVM.

- [x] Modello `Habit` (nome, tipo `boolean|counter|avoid`, assert `target/unit/step`, schedule, reminder, emoji, posizione, `createdAt`/`archivedAt`) e `Check` (habitId, data locale, stato, valore, nota, orario). Room `ThabitDatabase` v1: tabelle `habit`, `check` (PK composita `habit_id`+`date`: un secondo tap sostituisce il primo, non impila una seconda opinione sullo stesso fatto) e `day` (presenza) — **i verdetti non si persistono mai**. Date come stringhe ISO e non come epoch: ordinano lessicograficamente (le query a intervallo funzionano), sono leggibili in un dump e sono già la forma che scriverà l'export, così storage e file dell'utente non litigano su come si chiama un giorno. Schema esportato in `app/schemas/` e committato — le migrazioni future si scrivono contro una descrizione in repo, non contro la memoria di qualcuno
- [x] **`CheckState.PROGRESS` — quarto stato, deviazione registrata**: `- [ ] pushups  # 12/30` non è né un pass né un fail né un pending. `12` è un numero che l'utente ha digitato, quindi la riga esiste; ma il giorno è aperto e l'assert non regge ancora. Con i soli tre stati o si mentiva scrivendo `FAIL` su una giornata in corso, o si perdeva il progresso. `PROGRESS` è un **fatto, non un verdetto**: legge pending finché il giorno è aperto e fail quando chiude, ed è onesto anche nell'export. Il verdetto lo decide l'assert al momento della scrittura, così un target modificato dopo non rigiudica i giorni passati (§4.5: la storia resta con le regole del suo tempo)
- [x] **Presenza e `no run`**: riga `day` scritta dalla prima interazione **deliberata** (apertura app — `MainActivity.onStart`, non `onCreate`: un'app lasciata aperta a cavallo di `day_ends` è un giorno logico nuovo quando ci si torna). Insert `IGNORE` e non `REPLACE`, così `first_seen` resta la *prima* volta. Test: rientro dopo 9 giorni offline → 9 `NO_RUN`, **zero** build rosse inventate e zero commit; presenza scritta una volta sola al giorno; tap all'01:00 con `day_ends 03:00` attribuito al giorno logico precedente; e un test che verifica che *nient'altro* scriva presenza (aggiungere un test e spuntarlo non tocca la tabella `day`)
- [x] **`Schedule` puro** con forma canonica unica (`daily`, `mon,wed,fri`, `3/week`, `every 2d`) — la stessa nel file, in Room e nell'export; `parse` restituisce null su input sconosciuto invece di indovinare. `occursOn`/`dueCount` con test su bordi settimana, **anno bisestile** (intervallo che scavalca il 29 febbraio), creazione a metà finestra e archiviazione. **La quota `n/week` è stata prototipata per prima**, come deciso: regola fissata qui e rispettata da tutti i motori a valle — *una quota non fallisce mai un giorno, passa o fallisce la settimana ISO*
- [x] **Quota: tre decisioni prese scrivendola** — (a) gli **skip riducono il target** invece di far fallire la settimana (`target = min(n, giorni disponibili)`), coerente con "gli skip sono neutri ovunque": una settimana fuori casa con cinque giorni saltati chiede i due che restavano, non tre che non c'erano mai; (b) una settimana **senza nessun giorno girato è neutra** e non avanza pretese; (c) il target di una settimana **aperta non si accorcia a oggi** — un primo tentativo di clamp ai giorni già passati faceva risultare "met" un `3/week` il lunedì sera dopo una sola corsa, facendo sparire il test dal file per il resto della settimana. Bug trovato da un test e registrato qui perché è il tipo di errore che sul device si nota a settembre
- [x] **`DayBoundary`**: mapping istante→data logica, `startOf`/`endOf`. Test su DST 23h/25h (Europe/Rome, 29 marzo e 25 ottobre 2026), `day_ends` **dentro il buco** dello spring-forward (le 02:30 quel giorno non esistono: il confine slitta avanti col clock, che è la risposta onesta — il giorno è corto, non mancante), cambio timezone a parità di istante, e cambio di `day_ends` a giornata in corso che sposta *oggi* e non le righe già scritte
- [x] **`Verdicts` puro**: `TestState` (pass/fail/skip/pending/**holding**) e `BuildResult` con semantica Jenkins — `PASSED`/`UNSTABLE`/`FAILED`/`NOT_SCHEDULED`/`NO_RUN`. Le due risoluzioni al confine (pending→fail, holding→pass) *sono* il commit: una definizione, non una scrittura. Su un giorno aperto la formula è la stessa ma è graduato solo ciò su cui l'utente ha agito, così oggi legge "verde finora" invece di "fallito alle 08:00" — `DayRun.closed` dichiara che il verdetto è provvisorio e le schermate mostreranno i conteggi, non il badge. `NOT_SCHEDULED` copre anche "tutto saltato": non c'è niente da graduare, e `hasCommit` distingue i giorni che meritano una riga nel log
- [x] **Deviazione migliorativa registrata — un check esplicito conta anche su un giorno non dovuto**: `occursOn` risponde con lo schedule di *oggi*, quindi passare un test da `daily` a `mon,wed,fri` avrebbe cancellato dalla storia tutti i martedì già passati, contraddicendo §4.5. Ora una riga scritta dall'utente entra sempre nel run del suo giorno. La metà simmetrica (un giorno che *era* dovuto sotto la vecchia regola e non lo è più) resta non risolvibile senza versionare gli schedule: è la direzione indulgente, ed è registrata come limite noto invece che nascosta
- [x] **`Streaks` puro** con unità esplicita: giorni per gli schemi a giorno, **settimane per le quote** (l'unica lettura sensata, visto che una quota è giudicata sulla settimana). Skip neutri, `no run` che **rompe** la catena, pending di oggi che non la rompe (il giorno non è finito). Test dedicato al gaming che l'asimmetria rifiuta: nove giorni invisibili non tengono viva una streak
- [x] **`Health` puro (EMA)** — costanti fissate e documentate in VISION §7: **half-life 14 unità graduate**, `alpha = 1 - 2^(-1/14)` ≈ 0,0483, **seeding con il primo esito** invece che con zero (altrimenti un test passato 3 volte su 3 leggerebbe 13%, la stessa bugia che il clamp a `createdAt` esiste per evitare), skip e `no run` fuori dalla sequenza, nessun dato = **sconosciuto** e non 0%. Conteggio in *unità graduate* e non in giorni di calendario: un `mon,wed,fri` decade per corsa come un daily, essere programmati raramente non è una penalità. Il half-life è asserito da un test (14 fallimenti da 100% devono atterrare a metà strada), non solo scritto in un commento
- [x] **`Coverage` puro**: giorni dovuti effettivamente girati su giorni dovuti, **solo giorni chiusi** — oggi è escluso perché l'utente sta guardando lo schermo e contarlo gonfierebbe ogni finestra di un giorno di tautologia
- [x] **`Regressions` puro**, regola fissata accanto al half-life e documentata in VISION: ≥3 fallimenti sulle ultime 5 unità graduate dopo una corsa verde di ≥14 unità (stesso orizzonte dell'health: è lì che l'app è disposta a chiamare un'abitudine consolidata). Test che nessuna delle due assenze la può fabbricare — né una settimana di skip né una settimana offline
- [x] **`FlakyTests`**: pass rate 30d sotto il 60% con almeno 8 unità graduate, **esclusi i regression** (un test non può insieme essere stato solido e non esserlo mai stato). Il pavimento sui dati è metà della regola: senza, un test aggiunto due giorni fa e mancato una volta verrebbe accusato al 50%
- [x] **Finestre ancorate a `createdAt`** dentro `Outcomes.graded`, cioè in un posto solo: health, pass rate, regression e flaky lo ereditano tutti invece di ricordarselo ciascuno
- [x] `Records` puri: `longest-streak` (con gli estremi, così il log potrà puntare al commit giusto) e `perfect-week` — settimana ISO chiusa, tutto ciò che era graduato passato, **ogni giorno dovuto effettivamente girato** e ogni quota della settimana rispettata. Test esplicito del caso che la clausola di copertura esiste per rifiutare: due giorni visti e passati non fanno un 100%
- [x] `CommitHash` (SHA-1 di `thabit:<data>` troncato a 7 hex, `Locale.ROOT` — un hash che cambia in turco sarebbe un bug molto divertente da trovare in un file che non deve mentire), `SettingsStore` minimale (`day_ends`, `week_starts`, tema, editor; `restoreDefaults` che azzera la config **e nient'altro**), `ServiceLocator` a mano con `overrideForTests`
- [x] **`week_starts` non arriva a `IsoWeek` — decisione registrata**: l'impostazione muove le griglie di *visualizzazione* (colonne heatmap, tabella 7 giorni del README), mentre verdetti quota, separatori di settimana e `perfect-week` restano ISO (lunedì). Un record il cui significato cambia quando si gira una preferenza di display non sarebbe ricalcolabile dall'export
- [x] **`AmendWindow` pura**: scrivibili oggi e il giorno logico precedente, finestra che si chiude al `day_ends` di oggi (`remaining()` per la riga `# still editable until …` della Fase 6). Test sui bordi: `day_ends` non a mezzanotte, notte DST dentro la finestra (la grazia segue l'orologio, non un 24h scritto a mano), edit al secondo esatto della chiusura, e rientro dopo una settimana in cui è amendabile solo l'ultimo giorno chiuso. Il guard vive nel **repository**, non nelle schermate: nessun chiamante può retrodatare passando una data
- [x] **Un test aggiunto oggi non è amendabile su ieri** (conseguenza scoperta scrivendo i test del repository, e giusta): la finestra è aperta, ma la suite di ieri non conteneva quel test e retrodatarlo inventerebbe una corsa. `UNKNOWN_TEST`, non `WRITTEN`
- [x] **Skip con `until:` = una riga sola**, espansa in lettura da `SuiteHistory`. Materializzare una riga per giorno coperto sarebbe stato più semplice e avrebbe mentito due volte: sosterrebbe che l'utente ha interagito in giorni non ancora accaduti, e quelle righe farebbero risultare `ran()` giorni che l'app non ha mai visto — trasformando in silenzio una settimana fuori casa in una settimana di copertura piena. Query dedicata per le finestre aperte *prima* dell'intervallo caricato, o il lunedì successivo i giorni tornerebbero pending
- [x] **Rollover worker**: un solo job periodico (regola di serie) allineato al prossimo `day_ends`, `UPDATE` sulla policy così un edit di `day_ends` riallinea invece di impilare; safety-net a ogni `onStart` dell'app (telefono spento a mezzanotte, giorno DST da 23 ore, `day_ends` modificato). **Non scrive mai**: gli effetti sono iniettati (`RolloverEffects`, notifica Fase 9 + ridisegno widget Fase 10) e il test lo esegue contro un database vero verificando che non si sia mosso nulla — in particolare che non abbia timbrato presenza
- [x] Test completi su JVM: **166 test nuovi** (210 totali con quelli della Fase 1, tutti verdi) — 12 classi pure sul dominio, più repository/settings/ServiceLocator/worker su Robolectric con Room in memoria e DataStore su cartella temporanea. `lintDebug`, `assembleDebug` e `assembleRelease -PsignReleaseWithDebugKey` verdi

## Fase 3 — Schermata principale (`habits.test`)

- [x] **Split in due strati, deviazione migliorativa registrata**: `SuiteDocument` è il file come **valore puro** (nessun import Compose) e `HabitsTestScreen` lo disegna. Questa è la superficie più letta dell'app e ogni parola che ci sta sopra è una promessa — l'aritmetica dell'header, il dettaglio vivo di ogni commento, la prima frase dello stato vuoto — quindi vanno asserite carattere per carattere in test JVM normali, non cercate dentro un albero renderizzato. È lo stesso taglio del dominio, un piano più su
- [x] Rendering live: header `# suite <data> — N passed · M pending · K skipped` (`passed` sempre dichiarato anche a zero, il resto solo se è successo: una riga di zeri è rumore, non onestà) + riga `# logical day <data> — ends 03:00` **solo quando la data logica diverge da quella di parete**; una riga per test dovuto con checkbox, nome, emoji e commento `#` col dettaglio vivo
- [x] **`CodeFormat` nuovo** — la formattazione che la Fase 1 aveva rifiutato di importare da tsteps perché `UnitFormat` era dominio passi: numeri senza zeri di coda, frazioni `12/30`, orari 24h, percentuali, barra `▓▓▓▓▓▓▓▓░░`. Tutto `Locale.ROOT`, perché **un commento è sorgente**: un separatore decimale che segue la lingua del telefono metterebbe due numeri diversi nel file e nell'export per lo stesso fatto. Test dedicato che lo verifica con `Locale.ITALY` impostato
- [x] **La riga di test è un `WidgetLine` con due bersagli**, non un `CodeLine`: la riga porta **due** gesti (la casella esegue il test, il nome apre la scheda) e un solo `onClick` per riga avrebbe voluto dire sceglierne uno da perdere
- [x] **Difetto trovato e corretto — il bersaglio della casella si allarga col testo, non col padding**: `- [x] ` è un unico target di sei colonne monospaziate (~47dp a 13sp). Un padding orizzontale *prima* del trattino avrebbe spostato la riga rispetto alle righe di commento sopra, e una colonna che non si allinea è l'unica cosa che un editor di codice non può fare
- [x] **Target verticale ~30dp e non 48dp, scelta registrata**: righe alte abbastanza da soddisfare quel numero farebbero stare quattro test per schermata invece di dieci, costando all'app il colpo d'occhio su cui è costruita (§3.3.2). La metà orizzontale è comoda; se quella verticale serva più larga lo dice un pollice vero → in lista per la Fase 12
- [x] **Tap sulla casella**: boolean `[ ]`→`[x]` con l'orario stampato, tap di nuovo = undo; avoid `[·]`→`[!]` in un tap solo (rompere un avoid è raro, ma quando succede è un gesto, non un modulo) con `[note]` che compare **dopo**, così la nota resta davvero opzionale e insieme scopribile; counter → prompt in place
- [x] **Counter: il tap apre sempre il prompt, prefillato col valore che c'è già** (è una correzione, non una ridigitazione) e una **risposta vuota cancella la riga invece di scrivere uno zero** — "non ho inserito un numero" non è "non ne ho fatti". La virgola decimale si accetta in ingresso (chi scrive in italiano digita `1,5`) e si scrive col punto nel file, dove è codice
- [x] `[+1]` a fine riga **solo quando è davvero una scorciatoia**: al più `MAX_INCREMENT_TAPS = 12` tap dal nulla all'assert. Tredici tap sono un prompt, non una scorciatoia. Il token porta il passo vero (`[+10]`), non un `+1` che mentirebbe
- [x] **Espansione in place** al tap sul nome: `when:`, `assert:`, `remind:`, `streak:` con la sua unità (giorni, o **settimane** per una quota), `health: ▓▓▓▓▓▓▓▓░░ 82%` — e `--%` con `# not enough runs yet` quando l'app non sa ancora, mai uno zero finto. Azioni `[~ skip]` `[edit]` `[rm]`
- [x] **Difetto trovato da un test — la conferma di archiviazione sta su due righe**: `$ thabit archive "<nome>"` e sotto `# tap the command to confirm  [esc]`. Su una riga sola il comando spingeva `[esc]` fuori dallo schermo: la via d'uscita da una conferma distruttiva avrebbe richiesto uno scroll orizzontale per essere trovata. La via d'uscita resta sempre a schermo
- [x] **Skip con finestra come token ciclante** `[today] [3d] [1w] [2w]` su una riga di controlli sotto il prompt: la settimana fuori casa è **un'interazione** e scrive **una riga sola** (la Fase 2 la espande in lettura), non sette per test
- [x] **Deviazione registrata — un test non dovuto oggi è una riga commentata**, non nascosta e non una finta checkbox: `# deep work  — when: mon`. È la lettura che ne darebbe un editor di codice e tiene il file onesto due volte, perché il test è visibilmente ancora nella suite e visibilmente non chiede niente oggi, col motivo dichiarato invece che sottinteso. Collassate dietro `# N tests not due today — [show]`, con singolare e plurale giusti
- [x] **Empty state onesto** — e **`README_TAB_SHIPPED = false`, decisione registrata**: la riga `# the README tab says what a test is here` è spenta finché quella tab non esiste (Fase 7). Mandare un nuovo arrivato a una tab che non c'è sarebbe il file che mente, sulla primissima frase che l'app dice in vita sua. La costante si gira in Fase 7 e la riga torna; c'è un test su entrambi i rami
- [x] **contentDescription a parole, non a glifi**: ogni riga offre due bersagli parlanti — la casella dice la frase intera (*meditate 10 min, passato, alle 07:12*), il nome offre *Dettagli di …*. Niente merge della riga in un annuncio unico: due gesti, due annunci, ciascuno che dice cosa fa. 20 stringhe `cd_*` nuove in `values` e `values-it`; il dettaglio vivo viaggia strutturato (`RowDetail`) accanto al commento inglese, così **nessun fatto vive in una sola delle due metà** (§3.3.7)
- [x] Status bar `⎇ main | N tests` a sinistra e `X/N passed` vivo a destra (verde solo quando è pieno); entrambe portano la loro frase in chiaro per TalkBack, perché una decorazione git è firma e non informazione. **Nessun badge `✓`/`~`/`✗` su oggi**: il verdetto di un giorno aperto è provvisorio (Fase 2, `DayRun.closed`), quindi la schermata mostra i conteggi e lascia il badge al log
- [x] **`commentLine` ora può essere un controllo** (`onClick`/`onClickLabel`/`contentDescription`): `# 2 tests not due today — [show]` è una riga di sorgente che risponde a un tap. Migliorìa del kit, **candidata al backport** su tweather/tsteps
- [x] **`observeFullHistory()` senza finestra, motivazione registrata**: streak e health risalgono al `createdAt` del test, quindi una query a intervallo dovrebbe indovinare quanto indietro è abbastanza e troncherebbe in silenzio la streak più lunga della suite il giorno in cui supera l'indovinello. Un decennio di check sta in poche centinaia di kB (VISION §7): la query onesta è tutta la tabella
- [x] **Allineamento della colonna dei commenti non implementato — registrato**: il mock di VISION §4.1 allinea i `#` in colonna, ma la larghezza di un'emoji non è deducibile dal numero di caratteri e un padding calcolato sui caratteri disallineerebbe proprio le righe che l'emoji ce l'hanno. Due spazi, e si rivaluta sul device in Fase 12
- [x] Shell `ThabitApp`: la tab editor non è più il campione statico ma il `habits.test` vivo che legge il database e scrive i check dell'utente; le altre tre continuano a dire onestamente che il loro file non è scritto. FAB `+` col glow che, finché il wizard non c'è (Fase 5), stampa una riga transiente `$ thabit add — the wizard lands in the next phase` invece di non fare nulla
- [x] Test: **64 nuovi** (274 totali, verdi) — 27 sul documento puro riga per riga, 7 sulla formattazione, 17 sui gesti contro un database vero, 13 sul rendering e sulla metà parlata (inclusa la verifica in italiano con `@Config(qualifiers = "it")`). I test del view model **aspettano in tempo reale e non virtuale**: lo stato è alimentato da Room e DataStore, che lavorano davvero su thread propri, e un orologio virtuale li scavalcherebbe asserendo su un documento non ancora ricostruito — un test che passa per il motivo sbagliato oggi e mente su una regressione domani

## Fase 4 — Navigazione e Impostazioni (`settings.config`)

- [x] **Navigation Compose con le 4 route**, comportamento tab di serie: `popUpTo(start) { saveState = true }` + `launchSingleTop` + `restoreState`, così passare a Stats e tornare ritrova `habits.test` dov'era, e il tasto indietro di sistema passa dalla tab editor prima di uscire. **Grafo piatto e non annidato — decisione registrata**: nessuna tab ha ancora una seconda destinazione. Quando ne avrà una (la route di focus del log in Fase 6, i tag in Fase 8) gli stack per-tab diventano grafi annidati veri, che qui sono due righe e non un rifacimento. Costruire l'annidamento prima di avere qualcosa da annidare è macchinario comprato su previsione (§3.3.1)
- [x] **Ritoccare la tab su cui si è già non fa nulla**, nemmeno una ricomposizione del grafo: l'alternativa è un file che risale in cima ogni volta che un pollice sfiora la barra su cui si trova già
- [x] **`settings.config` diviso in due strati come `habits.test`**: `SettingsDocument` è il file come valore puro, `SettingsScreen` lo disegna. Le parole del file — chiavi, valori, hint, il commento della sezione non ancora cablata — sono asserite carattere per carattere in test JVM normali
- [x] Sezioni `suite` / `editor` / `theme` / `notifications` / `about` in JSON con commenti `//`, con i **valori come controlli**: i booleani si girano al tap, i cicli ciclano, il tema si sceglie toccando il profilo che si vuole nell'array `available_profiles` (`// active` su quello in uso). Niente switch, niente dialog, niente righe Material: il file **è** la schermata delle impostazioni
- [x] **`day_ends` è un ciclo e non un prompt — decisione registrata**: `day_ends` esiste per chi la giornata la finisce alle due di notte (§6.3), e ognuna di quelle risposte è un'ora tonda. Sei fermate (00:00–05:00) tengono la tastiera **fuori** dal file di configurazione, che è come si comporta la config di tutta la serie. Un `03:30` esatto è un caso che non ha chiesto nessuno; quando lo chiederà qualcuno, il prompt è a una riga di distanza. Il piano diceva "a cicli **o** prompt orario": questa è la metà che non aggiunge una tastiera a una schermata che non ne ha
- [x] **Un valore fuori dal ciclo avanza, non torna indietro di scatto**: un `day_ends` arrivato da un export o da una versione futura mantiene il suo posto e il tap successivo va alla fermata dopo, invece di riscriverlo in silenzio
- [x] **`week_starts` cicla lunedì/domenica/sabato e non tocca `IsoWeek`** (regola già fissata in Fase 2): muove le griglie di visualizzazione, non la definizione di settimana su cui si assegna un record
- [x] **Tema a runtime**: `MainActivity` legge il profilo dal `SettingsStore` e lo passa a `ThabitTheme`, quindi toccare `"dracula"` nel file ridipinge l'app al frame dopo, senza riavvio e senza uno stato tema separato. `LocalEditorOptions` è fornito **una volta sola dalla shell** per tutti i file: numeri di riga e a-capo sono proprietà dell'editor, non di una schermata (rimosso il provider che la Fase 3 aveva messo dentro la sua)
- [x] **`// Last modified:` timbrato in un posto solo** — dentro `edit()`, per cui un'impostazione aggiunta domani non può dimenticarsi di dichiarare che il file è cambiato. **Assente finché il file è quello che è arrivato** (un config mai toccato non è stato modificato, e mostrare una data di installazione sarebbe inventarsi una modifica) e **cancellato dal restore**: un file ripristinato non è un file modificato. È l'unico valore localizzato dentro il file, perché un timestamp è un dato e non sorgente
- [x] **Sezioni non cablate: commento onesto, mai interruttori morti.** `notifications` dichiara che arriva con la sua fase invece di mostrare tre toggle che non fanno niente — un config che mostra un interruttore sta promettendo che l'interruttore funziona. `$ thabit export --json|--csv` risponde `// nothing to export yet` invece di tacere
- [x] **`$ git restore settings.config` a due tap**, con `[esc]` su una riga sua (stessa lezione imparata dalla conferma di archiviazione in Fase 3) e con l'hint che dice **cosa non tocca**: `// resets this file only — the suite and its history are untouched`. Un verbo dall'aria distruttiva deve dichiarare ciò che non distruggerà. Toccare qualunque altro controllo disarma una conferma in sospeso, o un conferma armato dieci minuti prima sparirebbe addosso a un tap innocente
- [x] **Difetto trovato scrivendo i test — i controlli leggono il valore memorizzato, non quello disegnato**: la prima stesura ricavava lo stato corrente dal documento renderizzato, il che rende ogni tap dipendente dal fatto che il file sia già stato composto. Vero nell'app, ma un no-op silenzioso appena non lo è, ed è il tipo di accoppiamento che diventa un bug la prima volta che un controllo è raggiungibile da altrove (un'azione di notifica, il widget)
- [x] **Kit**: `rawValueLine` (valori non-stringa senza virgolette — mettere le virgolette a un booleano farebbe dichiarare al config un tipo che non ha) e `stringItemLine` (elemento di array tappabile); `contentDescription` aggiunto a `stringValueLine`. **Candidati al backport** su tweather/tsteps
- [x] **contentDescription a frasi**: `"day_ends": "03:00"` letto da TalkBack sarebbe punteggiatura JSON, quindi ogni riga-controllo porta la sua frase (*Il giorno finisce alle 03:00*, *Numeri di riga, disattivati*, *Tema dracula, in uso*), con orari e nomi dei giorni **localizzati dalla piattaforma** mentre il file resta inglese. 17 stringhe `cd_*` nuove in IT/EN
- [x] **Caccia ai test intermittenti — la parte più utile della fase.** I test dei view model aspettavano in tempo reale e fallivano circa una volta ogni cinque esecuzioni, ogni volta su un test diverso; le due cause erano vere entrambe: **(a)** view model, state flow, Room e DataStore giravano su scheduler diversi, quindi un tap seguito da un'asserzione era una corsa; **(b)** `Dispatchers.resetMain()` scattava mentre una coroutine era ancora in volo (`Dispatchers.Main is used concurrently with setting it`). Riscritti perché **un solo scheduler guidi tutto** — `StandardTestDispatcher` per il view model, per il `queryExecutor` e l'invalidation tracker di Room e per lo scope di DataStore — e il view model vive in un `ViewModelStore` vero, chiuso deterministicamente prima di restituire il main dispatcher. `ThabitDatabase.inMemory` ha guadagnato un parametro `executor` apposta. Un test rosso a caso è peggio di nessun test, perché insegna alla squadra a ignorare il rosso. **Sei esecuzioni consecutive della suite intera, tutte verdi**
- [x] Test: **40 nuovi** (314 totali, verdi) — 9 sul documento puro, 12 sui controlli contro un DataStore vero, 13 sul file renderizzato (inclusa la metà parlata in italiano), 5 sulla navigazione fra tab, e 1 sul dominio: **cambiare `day_ends` rietichetta oggi e non tocca le righe già scritte**

## Fase 5 — Il wizard (`$ thabit add` / `edit` / `[rm]`)

La suite si costruisce qui: è la fase che rende l'app usabile davvero.

- [x] **Wizard come sessione terminale**: `WizardDraft` è la conversazione come **valore puro** (default, cicli, validazione, l'`Habit` che ne esce), `WizardViewModel` la tiene e scrive una volta sola, `WizardScreen` la disegna. Stesso taglio in tre strati di `habits.test` e `settings.config`
- [x] **Tutto tranne `name:` ha un default** e `[done]` compare **appena il test ha un nome**: una risposta sola basta a creare un test (`boolean`, `daily`, niente emoji). `[more]` apre le domande rimaste. Sei prompt obbligatori sarebbero diciotto risposte nel minuto promesso da §9
- [x] **Difetto trovato da un test — le opzioni stavano fuori dallo schermo.** Il mock di VISION §4.5 mette gli schemi su una riga sola (`> when: [daily] mon..fri 3/week every 2d`): su un telefono la riga sfora il bordo destro, quindi la terza e la quarta opzione esistono solo per chi pensa a scorrere di lato. Nei primi sessanta secondi dell'app non è un dettaglio di layout, sono **opzioni che il lettore non scopre mai**. Ora una riga per opzione — e questo compra anche ciò che il mock non poteva contenere: **ogni** opzione ha accanto il suo modo piano, non solo quella scelta (§3.3.7). Stesso motivo per i giorni della settimana, spezzati in due righe (feriali e weekend): sette token tappabili su una riga sono più larghi di un telefono, e un giorno che non si vede è un giorno che non si può scegliere
- [x] **Etichette = i primi sessanta secondi**: `boolean # did you do it, yes or no`, `counter # count up to a number`, `avoid # something to stay away from`, `3/week # a number of times each week`; l'assert si legge come `[pages] >= [20]  # how much counts as done`, senza bisogno della parola *assert*. La metà parlata dice la stessa cosa nella lingua di chi ascolta: **nessuno si sente mai leggere "boolean" da solo**
- [x] **Il passo di un counter è sempre 1, e il wizard non lo chiede — decisione registrata**: `[+1]` compare così esattamente quando il bersaglio è a meno di una dozzina di tap (tre bicchieri d'acqua sì, venti pagine no). Il mock di §4.1 mostra `[+1]` su un test da 30 ripetizioni, che implicherebbe un passo da 10 che nessuno ha digitato: dedurre una "misura della serie" dal target metterebbe nel file un numero che l'utente non ha detto. Un passo per test è candidato per la fase di feedback sul campo
- [x] **`remind:` resta una riga di commento onesta** finché le notifiche non esistono (Fase 9): il transcript tiene la sua forma e nessuno si chiede dove sia finito il promemoria, ma la riga dichiara che non suonerà invece di offrire un controllo morto. Stessa regola della sezione `notifications` in Fase 4
- [x] **La sessione non si chiude dopo `[done]` — deviazione registrata e migliorativa**: VISION chiude con `✓ test added to the suite`, ma chiudere subito significa che nessuno legge quella riga, e "tre abitudini in meno di un minuto" (§9) è una promessa sulla **seconda e la terza**. Il transcript tiene le sue ricevute (`✓ "read 20 pages" added to the suite`) e offre `[+ another]`: il secondo test costa una risposta, non un giro attraverso il file e il FAB. In modalità edit invece `[save]` chiude, perché lì si era venuti per una cosa sola
- [x] **Qualunque transizione conferma prima il prompt aperto**: digitare un nome e andare dritti su `[more]`, `[boolean]` o `[done]` non lo perde. Perdere del testo digitato per una regola di focus che nessuno ti ha spiegato è il tipo di piccolo tradimento che rende un'app inaffidabile per motivi che l'utente non sa nominare
- [x] `[edit]` riapre lo stesso transcript **prefillato e già aperto** (chi arriva qui viene per cambiare una cosa precisa, non per una conversazione da due domande); `createdAt`, `position` e `archivedAt` non si toccano — un test non ringiovanisce perché gli hanno cambiato nome. **Cambi di schedule/assert valgono da oggi**: per l'assert è già vero senza fare nulla, perché il verdetto di un counter si scrive al momento del tap (Fase 2); per lo schedule resta il limite noto registrato in Fase 3 — un check esplicito conta sempre, quindi i passati non spariscono, mentre un giorno che *era* dovuto sotto la vecchia regola non è recuperabile senza versionare gli schedule, che è fuori scopo qui
- [x] **La tab editor diventa un grafo annidato**, esattamente al momento previsto in Fase 4: `editor/suite` e `editor/wizard` sono due schermate di una tab sola, quindi la barra resta al suo posto, la tab Editor resta accesa mentre il transcript è aperto (selezione per `hierarchy`, non per route) e indietro torna al file invece di uscire dall'app
- [x] **Bug vero trovato inseguendo un test intermittente — una scrittura non deve morire con la schermata.** `[done]` e `[esc]` possono distare millisecondi, e `viewModelScope` muore con la destinazione: la insert veniva cancellata a metà e il test appena descritto spariva. Aggiunto `AppGraph.appScope` per **le scritture avviate dall'utente che devono sopravvivere alla schermata che le ha avviate**; letture e stato restano in `viewModelScope`, dove cancellarli all'uscita è esattamente giusto. È il tipo di perdita di dati che si paga con una recensione da una stella
- [x] Ordinamento: i test nuovi in coda (`position` dal repository); riordino manuale rimandato — se servirà, sarà un verbo testuale, non drag-and-drop
- [x] Test: **54 nuovi** (368 totali, verdi) — 20 sulla conversazione pura, 14 end-to-end contro un database vero (i tre tipi, i quattro schemi, l'emoji, l'edit prefillato, l'edit di un test archiviato altrove), 17 sul transcript renderizzato (inclusa la metà parlata in italiano), 3 di navigazione che fanno il giro completo: FAB → nome → `[done]` → `[esc]` → il test è nel file, e `[edit]` → rinomina → `[save]` → il file è cambiato

## Fase 5.1 — Revisione di implementazione (fine Fase 5)

Giro di revisione sull'app costruita fino a qui, con l'occhio sulle funzionalità e sulla UI/UX invece che sull'architettura. Le cose trovate stavano quasi tutte **nell'ultimo centimetro, quello fra il dito e lo schermo**: il dominio e i tre strati reggono, ma un file che si legge benissimo aveva prompt che non prendevano il fuoco e una fetta di suite irraggiungibile per giorni interi. Registrate qui con la motivazione, come da regola del piano.

- [x] **I prompt aprono con il cursore dentro e la tastiera su.** In tutta l'app non c'era un solo `FocusRequester`, mentre **entrambi i gemelli** lo mettono a ogni chiamata (`StepsDocument.kt`, `RulesScreen.kt`): il tap sulla casella di un counter apriva `> pages: _` con l'underscore che lampeggiava e il campo spento, e ne serviva un secondo per digitare. Il caso peggiore erano i **primi sessanta secondi** dell'app, dove `> name: _` è la prima cosa che si vede. Il fuoco è finito **dentro `TerminalInput`** (`autoFocus`) e non a ogni chiamata come nei gemelli, perché così un prompt nuovo non può dimenticarselo — **candidato al backport**. Nel wizard è acceso solo quando il nome è la domanda aperta: sulla riga lasciata da un `[done]` il lettore sta guardando la ricevuta, non una risposta da dare
- [x] **Insets della tastiera (`imePadding`) sulla shell.** L'app è edge-to-edge e da targetSdk 35 il sistema **non ridimensiona più la finestra** per la tastiera: chi non dichiara dove sta l'IME se la ritrova sopra i propri prompt. Alza tutta la shell e non solo il file, così la barra resta raggiungibile mentre un prompt è aperto; e `EditorNavBar` passa da `navigationBarsPadding()` a `WindowInsets.navigationBars.exclude(WindowInsets.ime)`, o fra i tab e la cima della tastiera resterebbe una striscia morta alta quanto la barra di gesture. **Candidato al backport**
- [x] **Bug vero — la data di scrittura veniva dal documento disegnato.** `today()` leggeva `state.value.document?.logicalDate`, corretto solo finché il documento è fresco: con l'app aperta a cavallo di `day_ends` (le 23:58 di chi spunta le abitudini prima di dormire) il tap scriveva sul giorno **prima**, dentro la finestra amend e quindi senza un lamento, su un giorno che nessuno stava guardando. Ora ogni scrittura risolve la data da `repository.today()` (orologio + `day_ends`), e **se il file a schermo sta mostrando un altro giorno il tap non parte affatto**: la riga da cui viene descrive un giorno finito, quindi il file si ridisegna e lo dice (`# the day rolled over — this file is 2026-08-22 now`, che non è un `ERROR:` perché non è andato storto niente). Il secondo tap è ordinario. È la stessa regola che VISION §4.6 impone al widget — mai la suite di ieri travestita da oggi — applicata alla schermata principale
- [x] **Niente ticker sull'orologio, decisione registrata**: il ridisegno è un `MutableStateFlow` che si alza al ritorno in primo piano (`ON_RESUME`) e quando una scrittura scopre il salto di giorno. Un `flow` che emette a ogni confine sarebbe stato più elegante e avrebbe fatto girare all'infinito il `TestScheduler` dei test del view model: un loop di `delay` che si ri-arma non finisce mai sotto `advanceUntilIdle`, e un test che si pianta è un prezzo troppo alto per un caso che le due sveglie coprono già
- [x] **Un test non dovuto oggi era irraggiungibile — buco funzionale.** Le righe commentate erano `commentLine` senza `onClick`, e `[edit]`/`[rm]` vivono solo nell'espansione: un test `mon,wed,fri` di martedì, o una quota già soddisfatta, non si potevano né leggere né modificare né archiviare. Per una fetta della suite, per giorni interi. Ora la riga commentata si apre come tutte le altre e mostra **la stessa espansione** (`when:`, `assert:`, `streak:`, `health:`) con `[edit]` e `[rm]` — ma **senza `[~ skip]`**, perché saltare un test che oggi non chiede niente non vuol dire niente e scriverebbe una riga su un giorno che lo schedule non ha mai reclamato. `NotDueRow` porta quindi il suo `TestSpec`: "commentato, non nascosto" doveva già significare questo
- [x] **`[~ unskip]`: una finestra di skip si poteva aprire e non chiudere più.** `[2w]` toccato per sbaglio restava due settimane — lo stesso giorno il tap sulla casella la sovrascriveva, da domani in poi si poteva solo coprire un giorno alla volta, e i giorni futuri nemmeno quello. Ora lo stesso slot dei controlli mostra `[~ skip]` o `[~ unskip]` a seconda dello stato della riga. **`resumeSkip` cancella da oggi in poi e non riscrive i giorni già coperti**: la riga-finestra viene *accorciata*, mai cancellata, e resta l'**unica scrittura dell'app che tocca una riga fuori dalla finestra amend** — deliberata, perché `until` non è un risultato ma una **dichiarazione su giorni che non sono ancora successi**, e tornare a casa in anticipo da una settimana via dev'essere dicibile. Nessun giorno chiuso cambia verdetto, che è la promessa vera di §3.3.5
- [x] **Il canale dei commenti non si sente più due volte.** `# 12/30 reps` non aveva semantica, quindi TalkBack lo leggeva come nodo a sé — **in inglese, con voce italiana, ripetendo** il fatto che la casella aveva appena detto in italiano. Nuovo `Modifier.decorative()`, documentato una volta sola con la regola che codifica (**uno screen reader sente ogni fatto una volta**, da ciò che si può toccare), applicato ai commenti e agli hint del wizard e delle impostazioni. `hideFromAccessibility()` e **non** `clearAndSetSemantics`: il primo lascia il nodo dov'è, quindi le parole del file restano selezionabili a schermo e **asserite dai test** — cancellare la semantica le avrebbe nascoste anche alla suite che le sorveglia
- [x] **Plurali nella metà parlata.** `%d tests in the suite` e `%d times a week` dicevano *"1 tests"* e *"1 volte a settimana"*: il file visibile faceva il singolare a mano (`notDueComment`) e **la metà localizzata era meno corretta della sorgente**, che è l'inverso esatto della promessa §3.3.7. Passati a `<plurals>` `cd_status_suite`, `cd_when_quota` e `cd_when_interval` (con `once a week` / `una volta a settimana` e `every day` / `ogni giorno` come forme singolari vere, non come conteggi con un numero appiccicato)
- [x] **KDoc contraddittorio corretto**: `Check.until` dichiarava che i giorni coperti da una finestra «sono comunque materializzati uno per giorno», mentre `HabitRepository.skip` e `SuiteHistory` dicono l'opposto con tanto di motivazione. In un repo dove i commenti sono promesse è un bug a tutti gli effetti, ed era nel KDoc del modello: il primo posto dove uno va a leggere
- [x] **Deviazione registrata a posteriori — la barra `EditorTabs` non è in nessuna schermata.** Il componente è portato dalla Fase 1 e usato solo nelle preview, mentre nei gemelli **ogni** schermata ce l'ha in cima, anche quelle a file singolo (decisione pre-v1 registrata nel loro `EditorTab.kt`: erano gli unici posti senza indicatore sotto il file aperto, e a ogni switch sembravano un altro tipo di chrome). Qui il nome del file è la prima riga del documento e la barra arriva in **Fase 7**, quando `README.md` le dà finalmente due tab da mostrare; `settings.config` e `stats.md` prenderanno la striscia da un elemento come nei gemelli. Non era scritto da nessuna parte: adesso lo è
- [x] **`[esc]` chiede una volta sola, e solo quando c'è qualcosa da perdere — decisione di prodotto registrata.** Un draft non è un dato ma una conversazione, quindi far confermare l'abbandono di ogni frase incompiuta sarebbe attrito puro: un transcript vuoto, o un `[edit]` che nessuno ha cambiato, si chiude al primo tap. Quello che rende il secondo tap giusto è **dove sta il token**: `[esc]` è a otto punti da `[done]` sulla stessa riga, e un pollice che sbaglia perde tutto il digitato senza niente per rimediare. Quindi la domanda non è *"sei sicuro"* ma *"c'è qualcosa da perdere"* — il draft si confronta col punto da cui è partito (`baseline`: vuoto per un test nuovo, il test caricato per un edit). Niente dialog e niente comando `$` a due tap: quelli sono la forma per ciò che distrugge dati su disco, qui non c'è ancora niente su disco. Il file dichiara cosa farà il secondo tap (`# nothing is written yet — tap [esc] again to discard`) e `[esc]` armato si colora di rosso; toccare qualunque altro controllo disarma, come per il restore delle impostazioni
- [x] **Le transienti si stampano sotto la riga che le ha prodotte — decisione di prodotto registrata.** Erano in coda al file, quindi in una suite più lunga di uno schermo erano tecnicamente presenti e praticamente invisibili — e il messaggio nuovo del cambio di giorno è proprio quello che *deve* essere letto, perché spiega un tap che sembra non aver fatto niente. Scartato l'auto-scroll (sposta il file sotto il pollice, e allontana dalla riga che si stava toccando) e scartata la status bar (65 caratteri non ci stanno, e troncare un messaggio onesto è peggio che non mostrarlo). La domanda giusta era **dov'è l'occhio quando la transiente scatta**: sulla riga appena toccata. Quindi `SuiteMessage` porta l'id del test e la riga si stampa lì sotto, come fa già il prompt; solo un messaggio senza una riga a cui appartenere — o su un test che oggi non è più dovuto — finisce in coda al file
- [x] **Due difetti piccoli chiusi**: `plainHint()` nel wizard era codice morto dalla Fase 5, e il prompt di un **counter skippato** si apriva come `> value:` invece di `> pages:` perché l'unità veniva letta dal `RowDetail` del giorno. L'unità è una proprietà del test e adesso viaggia su `TestRow`: un counter saltato conta ancora pagine
- [x] Test: **20 nuovi** (388 totali, verdi) — 3 sul repository (`unskip` che accorcia una finestra e lascia skippati i giorni già coperti, `unskip` lo stesso giorno, `unskip` che non trova niente da disfare), 5 sul view model (`unskip`, il tap dopo il cambio di giorno che non scrive e ridisegna, il ritorno in primo piano che rilegge l’orologio, il counter saltato che sa ancora cosa conta), 4 sul wizard (`[esc]` che chiede solo quando c’è qualcosa da perdere, il transcript vuoto che si chiude al primo tap, la conferma disarmata da qualunque altro tap, la scrittura che non avviene comunque), 8 sul rendering (l’espansione di un test non dovuto e i controlli che offre, `[~ unskip]` al posto di `[~ skip]`, il commento nascosto agli screen reader ma non ai test, il fuoco sul prompt del nome e la riga dopo un `[done]` che non se lo riprende, la quota da uno al singolare, la transiente stampata sotto la sua riga e quella senza riga che cade in fondo, la riga di conferma di `[esc]`)

### Rimandato, con la motivazione

- **Nome del test senza limite di lunghezza** (`onName` non ha un `.take(n)`): con `word_wrap: false` un nome lungo spinge `# commento`, `[+1]` e `[note]` oltre il bordo destro, ed è **la stessa classe di difetto** che la Fase 5 ha corretto nel wizard (opzioni che esistono solo per chi pensa a scorrere di lato) — qui però su dati dell'utente. Va deciso sul device: un cap sul nome, un `word_wrap` acceso di default, o l'allineamento della colonna dei commenti già in lista. **Fase 12**
- **`$ thabit export` mostrato ma non cablato**, mentre `notifications` nasconde le proprie chiavi con la motivazione che «un config che mostra un interruttore promette che l'interruttore funziona». Due standard nello stesso file; il comando però *risponde* invece di tacere, quindi non mente. Si chiude da sé in **Fase 11**, quando il writer esiste
- **Archiviazione senza marcia indietro**: nessun `unarchive` nel DAO e nessuna vista degli archiviati. I dati restano (è il punto di `[rm]`), ma per l'utente il test sparisce; la conferma a due tap attenua e non risolve. Da valutare come riga `# N tests archived — [show]` in coda al file, insieme all'export che li rende comunque visibili
- **Le transienti non hanno ancora la metà parlata localizzata**: sono output di terminale, quindi inglesi nel file per regola (§1.3), ma la loro versione per lo screen reader dovrebbe seguire la stessa strada delle righe (inglese a schermo, lingua del lettore nell’orecchio). Finora erano quasi irraggiungibili; il messaggio del cambio di giorno le rende reali. Serve un tipo di messaggio con la sua risorsa e i suoi argomenti invece di tre costanti String — piccolo, ma è un pezzo di design e non un rattoppo: **Fase 12**, con le notifiche già localizzate della Fase 9 a fare da modello
- **Denominatori diversi affiancati nella status bar**: `6 tests` (la suite) accanto a `3/5 passed` (i dovuti meno gli skip). Nessuno dei due mente, ma stanno vicini con lo stesso peso visivo e invitano a leggerli come lo stesso numero. **Fase 13**, col resto del restyling
- **Undo di un counter a tre gesti** (apri il prompt, svuota, `[ok]`) contro l'unico del boolean: un `[clear]` nella riga di controlli del prompt lo pareggerebbe. **Fase 12**, insieme alla domanda sul bersaglio verticale

## Fase 6 — Storico (`habits_history.diff`)

- [x] **Tre strati come le fasi prima**: `LogDocument` è il file come **valore puro** (commit, verdetti, settimane, tag), `LogViewModel` tiene lo stato e scrive gli amend, `LogScreen` disegna. Niente è persistito: ogni commit, ogni verdetto e ogni percentuale di settimana si ricava dalle righe di check in lettura, ed è esattamente ciò che rende `--amend` gratis — cambi una riga e tornano ricalcolati il giorno, la sua settimana e i tag sopra di esso
- [x] Oggi in testa come `# On branch main — changes not yet committed (today)` col riassunto del run; un commit per giorno con hash stabile (`CommitHash`, già in Fase 2), `Date:` **localizzata**, messaggio `suite: N/M passed · K skipped`, build result con semantica Jenkins e colori (`✓` verde / `~ unstable` arancio / `✗` rosso). **Nessun commit nei giorni `no run`**: Jenkins non colora di rosso i giorni in cui nessuno ha pushato, e la storia non guadagna righe che l'utente non ha scritto
- [x] **Due denominatori diversi, entrambi dichiarati — decisione registrata**: il messaggio conta tutti i test che il giorno chiedeva (`suite: 2/3 passed · 1 skipped`), il verdetto conta solo quelli giudicabili (`✓ build passed (2/2 · 1 skipped)`). Non è una svista: gli skip lasciano il denominatore del verdetto ma non spariscono dal riassunto del giorno, e mettere in fila due numeri diversi senza spiegarli sarebbe stato il modo più veloce per far sembrare il file sbagliato. L'aritmetica viaggia sempre col verdetto (§3.3.7)
- [x] Espansione al tap: una riga per test — `+` verde (pass, con orario o valore), `-` rossa (fail, e un counter tiene il numero che l'utente ha davvero digitato: `- [ ] read 20 pages  # 12/20 pages`), `~` grigia (skip con nota); i cambi di suite come righe di diff (`+ test added: "journal"` / `- test archived: "cold shower"`), perché è l'unico posto in cui la storia spiega perché un test compare o smette di comparire
- [x] **Separatori settimana ISO sotto il blocco di giorni a cui appartengono — decisione registrata**: il file si legge verso il basso andando indietro nel tempo, quindi il separatore *chiude* la settimana come un `---` chiude una sezione, e l'occhio incontra prima i giorni e poi la loro aritmetica. Porta pass rate e delta contro la settimana precedente (`--- week 34 · 89% passed (+6% vs week 33) ---`, delta in verde o rosso); la prima settimana del log **non ha delta**, invece di inventarne uno contro una settimana che non è mai esistita
- [x] **I verdetti quota vivono sul separatore**: `quota: run 5k 2/3 ✗` — una quota non fallisce mai un giorno. Una settimana **ancora aperta e in ritardo non prende nessun glifo**: non ha fallito, semplicemente non è finita; una settimana che nessun giorno ha girato non avanza pretese (già garantito da `QuotaWeek.neutral` in Fase 2)
- [x] **`--amend`**: il run di ieri espanso risponde agli stessi tap di oggi — la casella scrive, un counter apre il suo prompt. Il commit di ieri porta `# still editable until 03:00` con **orologio localizzato**: la finestra si dichiara e non si scopre, perché una riga tappabile è invisibile e nessuno tocca una riga sperando che si apra. Da due giorni indietro la riga è disegnata **identica** e semplicemente non risponde: la storia non deve sembrare rotta, deve essere finita
- [x] **`day.amended`, colonna nuova e migrazione v1 → v2 — decisione registrata**: il marcatore sta sul **giorno** e non sulle righe di check, perché è permanente. Amendare ieri e poi disfare l'amend deve lasciare il commit che dice `# amended`: una bandierina sulle righe sparirebbe con l'ultima riga che la porta, e un commit che smette di ammettere di essere stato modificato è il file che mente su sé stesso. `markAmended` è una **UPDATE e mai una insert**: creare una riga `day` adesso rivendicherebbe una presenza che non c'è mai stata e trasformerebbe un giorno `no run` in un giorno girato (§7)
- [x] **Migrazione scritta e testata, non sperata**: `MIGRATION_1_2` gira in un test contro una tabella `day` v1 vera, costruita col `CREATE TABLE` copiato dallo schema committato — che è esattamente il motivo per cui `app/schemas/` sta nel repo. Nessun `fallbackToDestructiveMigration`: i check in quel database sono la storia dell'utente
- [x] **L'espansione del log offre la casella e il prompt di un counter, non `[~ skip]` / `[edit]` / `[rm]` — decisione registrata**: VISION dice «stessi tap di oggi», e i tap di oggi sono i risultati. Modificare la *specifica* di un test è una cosa del presente (vale da oggi in poi, Fase 5) e non appartiene a un giorno chiuso; offrirla qui avrebbe suggerito che si può cambiare com'era un test allora, che è precisamente la bugia che l'immutabilità della storia esiste per impedire
- [x] **Un tag `longest-streak` vuole una streak vera (`MIN_TAGGED_STREAK = 2`) — difetto trovato da un test**: con la soglia a uno, il primo giorno di chiunque si prendeva il tag, e un record che è anche un pareggio fra tutti i giorni che la suite ha mai visto non è un record, è una scelta arbitraria fra pari. `perfect-week` finisce invece sul commit più recente della settimana che l'ha guadagnata, cioè il giorno in cui è stata completata
- [x] **Le righe del log dicono il loro dettaglio anche allo screen reader**, al contrario del canale commenti di `habits.test` (Fase 5.1): ogni dettaglio qui è un orologio, un numero o **parole dell'utente** (l'unità che ha digitato, la nota che ha scritto). Non c'è inglese che abbiamo scelto noi, quindi leggerlo ad alta voce è leggere al lettore i suoi stessi dati
- [x] **Data del commit e orario della finestra non pre-formattati nel documento**: sono chrome, quindi il valore viaggia come `LocalDate`/`LocalTime` e lo formatta lo schermo nella lingua di chi legge. È l'unica eccezione alla regola "il documento tiene le parole esatte", ed è dichiarata dove succede
- [x] Tag sui record in arancio; status bar `⎇ main | N commits` a sinistra e `HEAD → <hash>` a destra, **entrambi con la loro frase in chiaro** (un hash è firma, non informazione); empty state che dice **perché** è vuoto e non solo che lo è (`a day commits when it ends`)
- [x] **Stessa guardia della Fase 5.1 sul giorno stantìo**: se la finestra si chiude mentre il log è aperto, il tap non parte, il file si ridisegna e lo dice. Qui conta il doppio, perché quello che era ieri è appena diventato storia
- [x] Test: **36 nuovi** (424 totali, verdi) — 18 sul documento puro (i tre verdetti con la loro aritmetica, il giorno `no run` senza commit, le righe del diff, i cambi di suite, il separatore con delta e senza, la quota sulla settimana chiusa e su quella aperta, la finestra solo su ieri, il marcatore, i tag), 7 sul view model contro un database vero (l'amend che scrive e marchia, l'amend disfatto che **non** smarchia, il counter col prompt, la finestra che si chiude sotto il file aperto), 10 sul rendering (compresa la metà parlata in italiano) e 1 sulla migrazione

## Fase 7 — README.md del giorno (seconda tab dell'editor)

- [ ] `WorkspaceStore` portato (DataStore `workspace`, tab persistita, scroll separati)
- [ ] Prosa **completamente localizzata**: titolo = data estesa; `## Oggi` (cosa è passato e cosa resta, parole neutre); `## Stato` (build badge del giorno, streak degne di nota, health in parole solo se dice qualcosa); `## Settimana` (tabella 7 giorni via `MarkdownTable`, oggi in grassetto e vivo, giorni senza test dovuti come `—`); footer `*Calcolato sul dispositivo · N giorni committati*`
- [ ] **Questa tab è lo strato di linguaggio piano dell'app** (VISION §3.3.7, §4.1), non un vezzo di serie: qualunque termine gli altri file stiano mostrando adesso, qui è detto in una frase nella lingua del lettore — `~ build unstable` diventa "quattro test su sei passati", un flaky diventa un'abitudine che salti spesso, `[·]` diventa "qui non devi fare niente, a meno che tu non lo interrompa". Due vincoli perché non degeneri in manuale: la glossa vive **dentro la prosa che c'è già** (nessuna sezione glossario) e riguarda **solo i termini a schermo adesso**
- [ ] Test: localizzazione, omissioni oneste, tabella rettangolare, oggi vivo, **copertura del vocabolario**: per ogni termine CI che le altre schermate possono mostrare (`unstable`, `flaky`, `coverage`, `regression`, `no run`, `holds`, `amended`), la prosa di questa tab lo dice in parole quando quel termine è a schermo — il controllo verificabile del principio §3.3.7

## Fase 8 — Stats (`stats.md`)

- [ ] **Heatmap contributi 12 settimane** (riuso del dominio `Heatmap` di tsteps): intensità = frazione della suite dovuta passata quel giorno, bucket relativi ai quartili dell'utente, skip fuori denominatore, futuri vuoti, **giorni `no run` vuoti come i futuri** (non è un fallimento: è un giorno che non sappiamo), oggi vivo
- [ ] **`## coverage`**: `24 of 30 days ran · 6 days no run` con barra, e il commento che spiega il concetto una volta (`# a day with no run is not a failed build — it is a build that never started`). È la sezione che rende letterale il titolo del file (§4.3 si chiamava *coverage report* senza avere una coverage) e il contrappeso onesto a `no run`
- [ ] Tabella `## suite health`: per test — health %, streak corrente, pass 30d; ordinata per health discendente
- [ ] `## flaky tests`: pass rate 30d sotto soglia, col numero e un solo hint fisso e fattuale; sezione presente solo con dati sufficienti — mai un dito puntato, solo un report. **Tutte le finestre partono da `createdAt`**: un test aggiunto ieri non compare fra i flaky con un finto 3%
- [ ] `## regressions`: un test che *era* solido e si sta rompendo (`meditate 10 min — 43 days green, 3 of the last 5 red`), riportato una volta e senza consigli — distinto dai flaky, che non sono mai stati solidi
- [ ] `## tags`: `longest-streak`, `perfect-week` — righe cliccabili verso il commit nel Log (pattern `LogFocus`)
- [ ] Rendering markdown evidenziato, heatmap colorata a mano, footer onesto, status bar `⎇ main | ro`; empty state con griglia comunque visibile. I titoli di sezione e i commenti-hint restano inglesi (regola di serie, `strings.xml`): la lingua piana per `flaky`/`coverage`/`regression` la porta la tab README (Fase 7), e ogni numero mostrato qui viaggia con la sua frazione, che è la glossa che non ha bisogno di traduzione
- [ ] Test: geometria heatmap con denominatore-dovuti, tabelle, flaky con soglia dati, tag cliccabili, empty state

## Fase 9 — Notifiche

- [ ] Permission `POST_NOTIFICATIONS` runtime col pattern completo di serie nella sezione `notifications` (toggle gated, status line, detour impostazioni di sistema)
- [ ] **Reminder per test** (`remind:` dal wizard): sveglie **inesatte** via `AlarmManager.setWindow` (~10 min, niente permission exact-alarm — VISION §6.7: un reminder è un nudge, non una sveglia; dichiarato nel file), riarmate al boot e a ogni edit; notifica con **azione `pass`** per i boolean — il test si smarca dalla tendina (e il tap sull'azione scrive la presenza del giorno, Fase 2); i counter aprono l'app sul prompt. **Un solo alarm "prossimo reminder" riarmato è stato valutato e respinto** (motivazione in testa al file e in VISION §7): gli alarm inesatti sono batchati dall'OS, quindi non si risparmia batteria, e una catena che si riarma perde tutti i reminder su un fire mancato
- [ ] `daily_commit` (default on, canale LOW silenzioso): il build result del giorno chiuso, compatta una riga / espansa stile `git log` (pattern due-forme di tsteps). **Prosa localizzata, mai il token nudo**: è l'unico posto in cui un verdetto compare senza file intorno e senza aritmetica accanto, quindi `~` e i numeri restano ma la frase è nella lingua del lettore (*quattro test su sei passati*), non `build unstable` (pattern `notif_title_*` di tsteps, che localizza titoli e corpo)
- [ ] `pending_digest` (**default off, opt-in** — VISION §3.3.4): un solo riepilogo serale all'ora configurata (`✗ 3 tests still pending`), mai un nag per test
- [ ] Canali separati, tutte disattivabili, linguaggio neutro sempre; niente notifiche motivazionali, mai
- [ ] Test: contenuti puri delle due forme, azione pass end-to-end, digest solo opt-in, riarmo sveglie

## Fase 10 — Widget (`thabit --status`)

Architettura widget di serie ereditata intera (sizes-map, un gradino per riga, breakpoint misurati, `monospace` di sistema, opacità configurabile, zero polling).

- [ ] Content builder puro: prompt `$ thabit --status 🧪`, barra suite `▓░ N/M`, i test dovuti oggi in colonne compatte `[x] nome` (avoid con `[·]`: qui il glifo è l'unico canale, non c'è commento a disambiguare da un pending), commento `# N pending — tap to pass`; tier che tagliano dal fondo, il minimo è la sola barra suite
- [ ] **Check-off dal widget**: le righe boolean portano PendingIntent propri (action+requestCode distinti — lezione filterEquals di tsteps) e smarcano senza aprire l'app; counter/avoid aprono l'app sul punto giusto
- [ ] Ridisegni su: tap, rollover, modifiche suite/settings; il widget che non ha ancora ridisegnato mostra la data che sta renderizzando — mai la suite di ieri spacciata per oggi. **Un ridisegno non è presenza**: solo il tap scrive la riga `day` (Fase 2); un widget guardato e mai toccato non testimonia niente, e il rollover che timbrasse presenza da solo trasformerebbe ogni giorno a telefono acceso in un giorno "girato"
- [ ] contentDescription delle righe a parole e non a glifi, come nell'app (Fase 3): sul widget è ancora più necessario, perché qui non c'è nemmeno il commento a dare contesto
- [ ] Test: transcript e budget per tier, binary search sui gradini (pattern tsteps), colori via span attraverso l'IPC, intent per riga

## Fase 11 — Export dati

- [ ] `$ thabit export --json` e `--csv` in fondo a settings.config (pattern tsteps Fase 13 per intero: MediaStore pending→publish, zero permission storage, nome riportato = quello vero, esiti `// wrote …` / `// nothing to export yet` / `// ERROR:`)
- [ ] JSON un documento (suite + tutti i check + **tutte le righe `day` di presenza**, un record per riga); CSV tre file (`thabit-suite-*.csv` + `thabit-checks-*.csv` + `thabit-days-*.csv`); `Locale.ROOT`, valori canonici, half-life dell'health e regola delle regressioni dichiarate nell'header — tutto ricalcolabile, coverage e `no run` compresi (una statistica che l'utente non può verificare è una formula segreta)
- [ ] Onestà del formato: skip con nota, `# amended` come flag dato, test archiviati inclusi con `archived_at` (la storia è dell'utente)
- [ ] Formato documentato nel README (sezione Export); test su documenti e sink

## Fase 12 — Rifiniture dal campo

Fase cuscinetto deliberata: la serie ha imparato che il device del committente riscrive sempre qualcosa (status bar affamate, controlli che non si spiegano, notifiche da ridisegnare). Qui si raccolgono le tornate di feedback prima del restyling finale, ciascuna registrata con la motivazione.

- [ ] Tornata/e di feedback su device del committente
- [ ] Valutare qui, con dati d'uso reali: **il runner guidato `$ thabit run` con il FAB che diventa `▶`** (VISION §6.9 — la domanda da rispondere sul device: passare in rassegna i pendenti aggiunge qualcosa a spuntarli nel file, o è una terza superficie per gli stessi check?), **i profili di suite come branch** (§6.10 — dopo che il modello della suite ha dimostrato di essere stabile), gruppi per momento della giornata (sezioni YAML), riordino manuale, `$ thabit watch` (timer come processo) — si aprono solo se la pratica li chiede (VISION §6)

## Fase 13 — Restyling pre-v1 e Release

- [ ] Passata di coerenza design su tutte le schermate: vincoli, spazi, scala font di sistema, TalkBack (le righe si annunciano a parole, mai a glifi)
- [ ] **Audit del principio §3.3.7 schermata per schermata**: nessun termine CI è l'unico posto in cui un fatto esiste — ogni verdetto ha la sua aritmetica accanto, ogni termine a schermo ha la sua frase nella tab README, nessuna decorazione git porta un dato che non sia in chiaro altrove. È il controllo che rende l'accessibilità una verifica invece di una buona intenzione
- [ ] Edge case checklist su device: DST e cambio timezone, cambio `day_ends` a giornata in corso, riavvio (riarmo sveglie), giorno vuoto, suite vuota, amend al limite della finestra, archivio con storia lunga, battery saver
- [x] Keystore di release reale — **anticipato a prima della Fase 1** (ago 2026, in coda al giro che ha messo in riga tutte le app della serie): chiave RSA 4096 trentennale (alias `thabit`) generata dal committente con `keytool`, fuori repo in `C:\Fiorenzo\keys\` con backup nel password manager; 4 proprietà `THABIT_*` compilate in `~/.gradle/gradle.properties`, 4 GitHub Secrets caricati, `release.yml` su tag `v*` già nel repo dalla Fase 0. Verificato in locale: `assembleRelease` produce `app-release.apk` firmato, certificato controllato con apksigner (`CN=callbackdev`, fingerprint distinto dalle altre 4 app); il percorso `-PsignReleaseWithDebugKey` è quello già esercitato dalla CI verde. Il keystore di debug committato è nuovo e unico (alias `thabit-debug`, fingerprint diverso dai gemelli)
- [ ] Versione 1.0.0, `CHANGELOG.md`, screenshot reali in `docs/screenshots/`, README definitivo, tag e prima GitHub Release verificata

---

## Note trasversali

- **Vincoli di design non negoziabili** (vedi `CLAUDE.md` e VISION §1.2): solo JetBrains Mono (eccetto widget), griglia 4px, indent 20px, niente ombre (bordi 1px + glow del FAB), raggio 4px ovunque, controlli renderizzati come testo, emoji come icone nel testo.
- **Regola l10n**: il "codice" resta inglese (chiavi YAML/JSON, filenames, commenti, output terminale, hash, check line); chrome e valori-dato localizzati IT/EN; i nomi dei test sono dati dell'utente. `README.md` (la tab) è prosa: localizzata per intero.
- **Niente rete**: se una feature futura chiede la permission INTERNET, non è una feature di thabit.
- **Verdetti mai persistiti**: build result, streak, health e record si calcolano in lettura dai check. Il worker di mezzanotte notifica e ridisegna, non scrive.
- **L'unica altra scrittura è la presenza**: la riga `day` della prima interazione deliberata della giornata. Non è un verdetto ma una prova, ed è ciò che rende dicibile `no run` — un giorno che l'app non ha visto è ignoto, non fallito: niente commit, cella vuota, fuori da ogni denominatore, health neutra, streak interrotta.
- **La metafora non è un pedaggio** (VISION §3.3.7): nessun termine CI è l'unico posto in cui un fatto esiste — i verdetti viaggiano con la loro aritmetica, e la lingua piana vive nelle tre superfici localizzate della serie (tab README, notifiche, contentDescription). I commenti dei file finti restano inglesi: è per questo che quelle tre superfici devono essere complete.
- **Il file principale è `habits.test`**, non `.yaml`: la sintassi è YAML-flavored, il nome non promette una grammatica che `- [x] nome` non rispetta.
- **Ordine**: Fasi 1–2 sono il fondamento (la 2 in parallelo alla 1). La 3 dipende da 1–2; la 5 rende l'app usabile e sblocca il valore di 6–10; il widget (10) dopo che dominio e settings sono stabili.
- **Import dalla serie**: i componenti si copiano adattando il package, mai linkando i repo; ogni divergenza che emerge (bug fixati qui, migliorie) va valutata per il backport a tweather/tsteps.
- Aggiornare questo file smarcando i passi completati e annotando le deviazioni dalla VISION con la motivazione.
