<?xml version="1.0" encoding="utf-8"?>
<!-- updatePeriodMillis=0 on purpose: the widget never polls on its own. It is
     re-rendered by whoever moves the state it shows — the rollover at day_ends,
     a check written from the shade or from the widget itself, the app while it
     is in front — so it costs nothing over what those already spend.
     targetCellHeight=3 so the default placement lands in the medium band the
     picker preview advertises. No configure activity: thabit has one suite,
     there is nothing to pin. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_description"
    android:initialLayout="@layout/widget_thabit_medium"
    android:minWidth="110dp"
    android:minHeight="48dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="48dp"
    android:previewLayout="@layout/widget_thabit_preview"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="4"
    android:targetCellHeight="3"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
